# T04 陌生智能体冷启动验证

## 文档状态

- 当前阶段：协议、冻结输入、首次试做原始事实、补充样本、发现分类、PLAN 1.0.2/1.0.3 修订、失败复验证据及负责人风险接受均已记录；T04 审计完成。
- 本文保留可复现协议、输入证据和待执行模板；第 11–12 节保存原始事实，第 13 节另行分类，避免用事后解释改写首次回复。
- G3 状态：已通过（项目负责人风险接受例外，2026-07-18 01:33:39 +08:00）。

## 1. 智能体类型与差异

| 角色 | 供应商 | 产品类型 | 智能体/界面 | 当前证据状态 |
| --- | --- | --- | --- | --- |
| 主开发智能体 | OpenAI | Codex 桌面应用中的编码智能体 | OpenAI Codex App | 已由本项目当前开发会话确认 |
| 陌生智能体 | Google | Gemini 网页版通用对话智能体 | Google Gemini 网页版；用户当前称界面为“Gemini 3.5 Flash” | 仅为执行目标和用户提供的当前称呼，尚未截图或核验完整模型名称 |

二者供应商不同，产品类型也不同。执行当天必须以 Gemini 网页实际显示的完整模型名称为证据，并记录显示位置、验证时间和截图/导出文件名；在完成该核验前，不得把“Gemini 3.5 Flash”或任何其他模型标签写成已验证事实。

## 2. 全新会话与无历史验证

### 执行步骤

1. 在 Gemini 网页版创建一个全新会话，不复用任何既有对话。
2. 执行前检查该会话消息区为空，且没有从旧会话复制内容；不得导入聊天历史、项目摘要或隐藏上下文。
3. 不使用 Gemini memory；若界面提供 memory、个性化或已保存信息开关，执行前关闭并保存界面证据。若界面无法关闭或无法判断，暂停验证并把它记录为环境缺口，不得宣称“无 memory”。
4. 只上传第 3 节冻结的 `SPEC.md` 和 `PLAN.md`，随后发送第 5 节的完整原始 prompt；不追加口头解释。
5. 保留能够同时证明“全新会话”“实际完整模型名称”“memory/个性化状态”和上传文件列表的截图或导出记录。

### 执行时填写的证据

- 会话创建与验证时间（含时区）：`执行时填写`
- 网页实际显示的完整模型名称：`执行时填写`
- 模型名称显示位置：`执行时填写`
- 全新会话验证方法及结果：`执行时填写`
- memory/个性化检查方法及结果：`执行时填写`
- 截图或导出文件名及保存位置：`执行时填写`
- 操作者：`执行时填写`

## 3. 冻结输入证据

- HEAD/输入提交：`b337e5ef3decb6f71a5216922f94feee05c3b2d0`

| 文件 | 输入 commit | Git blob hash | SHA-256 | 行数 |
| --- | --- | --- | --- | ---: |
| `SPEC.md` | `b337e5ef3decb6f71a5216922f94feee05c3b2d0` | `5bc380d4bf5854a07d0f65e06c517ffaa81cfd66` | `a22e11b49d489ac81d7dbfa08d8c3746a89669d6fedfaba0d42a2e88f19c2f6f` | 1305 |
| `PLAN.md` | `b337e5ef3decb6f71a5216922f94feee05c3b2d0` | `8690e3bd56894fffd7b64d4c5b2ed6c253ac2b38` | `1f88a5f9b36634200ece0198cf9dcae611cc0cd1fef918475b700c498c378a94` | 1174 |

执行前必须重新计算并逐项比对；任一 commit、blob hash、SHA-256 或行数不一致时停止，不得上传替代文件。

## 4. 唯一输入文件清单

提供给陌生智能体的文件严格且仅有：

1. `SPEC.md`
2. `PLAN.md`

不得提供仓库、历史聊天、memory、`AGENT_LOG.md`、`SPEC_PROCESS.md`、`guiding.md`、课程任务清单、主控预审结论、项目摘要、截图说明或任何口头补充。不得要求、提供或记录 API key。发送 prompt 后如需澄清，只允许逐字保存陌生智能体的原始问题；在首次试做结束前，主控不得提示已知答案或引导其绕过疑问。

## 5. 完整原始 prompt

以下代码块内容必须原样发送，不得在发送前后补充说明：

```text
你正在进行一次陌生智能体冷启动验证。你只能使用本会话上传的 SPEC.md 和 PLAN.md；不要假定你知道任何项目历史、聊天上下文、memory、仓库状态或未提供的文件，也不要索要或输出任何 API key。

请先阅读这两个文件，然后自行选择 PLAN.md 中 1–2 个 Task 尽可能试做，并逐条说明选择的 Task 及选择理由。严格遵守 PLAN.md 的依赖、文件边界、步骤、测试和停止条件。

遇到任何不确定、歧义、缺失文件、缺失工具、无法验证的环境条件或相互冲突的要求时，立即暂停当前步骤并逐条提问，不得猜测、补全或绕过。每个问题请保留：所选 Task、触发的文件与行/章节、你的原始理解、具体不确定点、暂停位置、继续所需的信息。

如果网页环境没有本地终端、Git 仓库、文件写入或命令执行能力，请如实列出你实际可用和不可用的工具，把它作为环境限制报告；不要伪造命令、测试、文件修改、diff 或运行结果。在能力范围内继续做可验证的分析或产物草案，但不得声称未实际执行的步骤已经完成。

试做结束时，请列出：实际可用工具、所选 Task 与理由、已完成步骤、未完成步骤、所有原始问题与暂停点、实际尝试的命令及其真实观察（如无执行能力则明确写“未执行”）、错误理解及其纠正来源、停止原因、产物清单或 diff（如无则明确写“无”）。不要提交或合并正式源码。
```

## 6. 环境、隔离与产物规则

- 试做发生在 Gemini 全新网页会话中，不在当前 Git 工作区内执行；网页会话与正式开发分支隔离。
- 执行时逐项记录 Gemini 网页实际可用工具，例如文件上传、文本回复、可下载附件、代码执行、终端、Git、仓库访问和联网能力；未知能力不得推定为存在。
- Gemini 网页可能没有本地终端或仓库。缺少终端、Git、文件写入或测试运行能力时，分类为“环境缺口”，如实记录“未执行”及缺失能力，不得伪造命令执行、测试通过、文件改动或 diff。
- 即使工具受限，仍要求陌生智能体从 PLAN 中选择 1–2 个 Task，在能力范围内尽可能试做；一旦不确定立即暂停并逐条提问，不得猜测。
- 允许保留的产物仅限：完整对话导出、证据截图、陌生智能体生成的文本/附件、原始问题记录和人工保存的只读 diff。保存位置和文件 hash 在执行提交中填写。
- 禁止把任何试做源码、配置、测试或依赖合入当前分支，禁止据此宣称 T05 已开始。后续若需清理，只能逐个删除明确路径的临时文件；不得批量删除文件或目录。

## 7. 原始问题记录模板

每个问题单独复制原文，不得用整理后的结论替换：

| 字段 | 记录 |
| --- | --- |
| 问题编号 | `执行时填写` |
| 提问时间（含时区） | `执行时填写` |
| 所选 Task | `执行时填写` |
| 触发文件与行/章节 | `执行时填写` |
| 原始问题（逐字） | `执行时填写` |
| 提问前的原始理解 | `执行时填写` |
| 暂停点/当前步骤 | `执行时填写` |
| 暂停状态 | `已暂停 / 未暂停；执行时选择` |
| 是否继续及依据 | `执行时填写` |
| 主控回复（逐字；首次试做结束前原则上不答） | `执行时填写或“无”` |
| 关联证据 | `执行时填写` |

## 8. 结果分类模板

允许的分类只有：`SPEC 缺陷`、`PLAN 缺陷`、`两者不一致`、`环境缺口`、`智能体误读`。每条发现单独填写：

| 字段 | 记录 |
| --- | --- |
| 发现编号 | `执行时填写` |
| 关联原始问题/产物 | `执行时填写` |
| 分类 | `执行时从五类中选择` |
| 事实证据（文件、行/章节、原文、截图） | `执行时填写` |
| 预期行为 | `执行时填写` |
| 实际行为/观察 | `执行时填写` |
| 负责人判断与理由 | `执行时填写` |
| 是否修订及修订位置 | `执行时填写` |

分类规则：网页缺少本地终端、Git 或仓库访问属于环境缺口，不能因此虚构命令结果；只有文本证据支持时才可判为智能体误读；主控预审中已知的 T03 问题不得冒充冷启动发现。

## 9. 预期差距比较模板

| 比较项 | PLAN 预期（文件/行/章节） | 试做事实与证据 | 差距 | 是否阻塞新鲜 subagent | 后续动作 |
| --- | --- | --- | --- | --- | --- |
| 文件/路径 | `执行时填写` | `执行时填写` | `执行时填写` | `执行时填写` | `执行时填写` |
| 接口/数据结构 | `执行时填写` | `执行时填写` | `执行时填写` | `执行时填写` | `执行时填写` |
| 测试/验收 | `执行时填写` | `执行时填写` | `执行时填写` | `执行时填写` | `执行时填写` |
| 命令/工具 | `执行时填写` | `执行时填写` | `执行时填写` | `执行时填写` | `执行时填写` |
| 依赖/顺序 | `执行时填写` | `执行时填写` | `执行时填写` | `执行时填写` | `执行时填写` |
| 停止条件 | `执行时填写` | `执行时填写` | `执行时填写` | `执行时填写` | `执行时填写` |

## 10. 首次试做待执行清单

- [ ] 核验并保存网页实际显示的完整模型名称。
- [ ] 建立全新会话并保存无历史、无 memory 的检查证据。
- [ ] 重新核对冻结输入的 commit/blob/SHA-256/行数。
- [ ] 确认上传列表严格只有 `SPEC.md` 和 `PLAN.md`。
- [ ] 原样发送第 5 节 prompt，保存完整对话和工具能力记录。
- [ ] 原样记录 Task 选择、问题、暂停点、尝试、观察、停止原因和产物。
- [ ] 后续独立提交中完成分类与预期差距比较；本提交不预填结果。

## 11. 陌生智能体首次试做：原始事实记录

本节仅记录收到的网页截图、文件元数据和 Gemini 原始回复。除“协议符合/未证实事实”清单外，不在本提交对问题作分类、判断规约缺陷或修订 `SPEC.md`、`PLAN.md`。

### 11.1 证据元数据

| 证据 | 路径 | 文件大小 | 创建/修改时间 | SHA-256 | 直接可见事实 |
| --- | --- | ---: | --- | --- | --- |
| 截图 1 | `C:\Users\32175\AppData\Local\Temp\codex-clipboard-96984d2a-99ea-45ed-a44a-ff4d6d5e9cdb.png` | 1464728 bytes | 2026-07-17 23:33:16 +08:00 | `2e5e9b874fdd1403a9d3cc2808c9202bfba0f0355ac3d597e22eadde35d69461` | Gemini 网页界面显示空白新对话页面；模型下拉标签显示为“Flash”。 |
| 截图 2 | `C:\Users\32175\AppData\Local\Temp\codex-clipboard-436a7bd1-b543-4b54-9888-4f95a556e9a3.png` | 1151308 bytes | 2026-07-17 23:34:41 +08:00 | `2dd7965d15994047cb760ece6141275cacf5d604e9251dc9e043463147fbb2f9` | Gemini 网页界面的输入框显示附加的 `PLAN` 与 `SPEC` 文件卡片；模型下拉标签显示为“Flash”。 |

- Codex 收到上述证据时间：2026-07-17 23:36:36 +08:00。
- 真实发送时间、真实响应开始时间和真实响应结束时间均未被直接记录；本节只记录截图时间和收到证据时间。

### 11.2 协议符合与未证实事实

**与协议相符的已记录事实（不构成后续分类结论）：**

- 截图 1 显示空白新对话页面。
- 截图 2 显示附加 `PLAN` 与 `SPEC` 文件；未从该截图推定其他输入内容。
- 网页界面截图中的可见模型标签是“Flash”。这不足以核验完整模型名称，尤其不得据此写成已验证的“Gemini 3.5 Flash”。
- Gemini 原始回复选择了 `T05：建立工程骨架和测试入口`，并列出选择理由、两个暂停点、命令“未执行”、停止原因和“无”产物。
- Gemini 原始回复自述实际可用工具为 `personal_context:retrieve_personal_data`，且本轮未消费；此项作为原始环境事实保留。

**尚未由现有证据证明的事实：**

- 截图没有证明 Gemini memory、个性化或已保存信息已关闭；不得声称 memory/个性化已关闭或“无 memory”。
- 截图没有核验网页实际显示的完整模型名称。
- Gemini 原始回复称“SPEC.md（未直接上传）”，而截图 2 显示附加 `SPEC`。两项事实并列记录，未在本提交判断何者为准。
- 未直接记录完整对话导出、实际发送的 prompt 全文、输入冻结 hash 的重新比对过程、操作人身份或真实响应起止时间。

### 11.3 原始回复（逐字保存）

> 以下为收到的 Gemini 原始回复。原有段落和列表结构保留；其中的自述不被本提交改写为已独立验证的事实。

## 一、 陌生智能体冷启动验证与环境限制报告

我已阅读并理解了本会话中提供的 `SPEC.md`（未直接上传，但其核心规约已在 `PLAN.md` 中以约束形式体现）与 `PLAN.md` (v1.0.1)。

根据冷启动验证的要求，由于当前网页环境**没有本地终端、Git 仓库、文件写入或命令执行能力**，现如实列出实际可用和不可用的工具，作为环境限制报告：

* **实际可用工具**：`personal_context:retrieve_personal_data`（用于检索用户相关上下文）。
* **不可用工具**：本地文件读写、`bash` / 终端命令执行、Git 仓库操作。

因此，我将无法实际执行代码的红-绿-重构循环（RED/GREEN/REFACTOR），无法通过真实命令生成锁文件或运行测试。在能力范围内，我将通过**纯逻辑验证和静态产物草案设计**，对任务进行试做。

---

## 二、 试做 Task 选择及理由

根据 `PLAN.md` 的依赖 DAG、文件边界与停止条件，我选择以下 **1 个 Task** 进行试做分析与规约评审：

### 1. 选择的 Task

* **T05：建立工程骨架和测试入口**


### 2. 选择理由

* **依赖序位最前**：根据依赖 DAG（`T05 --> T06 --> T07 ...`），T05 是整个决策感知型 Coding Agent Harness 实现计划的绝对起点。


* **暴露规约缺陷与冷启动验证**：T04 属于冷启动与暴露规约缺陷阶段，而 T05 是工程实现的第一步。选择 T05 能够最直接地评审在没有任何项目历史的前提下，初始化一个 TypeScript monorepo 严格工作区所需的配置规约是否完备，从而暴露出可能存在的环境条件冲突（例如全局限制中的 G3 状态阻塞）。



---

## 三、 T05 试做步骤与触发暂停点提问

在进入试做规划时，我触发了 `PLAN.md` 规定的**立即暂停当前步骤并逐条提问**的停止条件。

### 暂停点 1：G3 闸口状态与实现权限冲突（停止原因）

* **所选 Task**：T05


* **触发的文件与行/章节**：`PLAN.md` -> `Global Constraints` 核心红线（"G3 通过前禁止创建本计划描述的源码、测试、依赖、Dockerfile 或 CI 文件。"）以及 `1. 文档控制与 Gate`（"G3 未通过"，"实现权限：未开放；T04 冷启动与 G3 通过前禁止实现"）。


* **我的原始理解**：冷启动验证要求试做 PLAN.md 中的 1–2 个 Task 尽可能试做，但全局硬限制（Global Constraints）和 1.1 规约严格禁止在 G3 通过前创建任何文件、依赖或测试代码。


* **具体不确定点**：本次冷启动验证是否代表项目负责人已授予突破“G3 未通过”限制的临时豁免权，还是本试做应该在 Step 1 验证 G3 证据时因为“G3 未通过”而直接宣告终止并走向“升级给人”的逻辑？


* **暂停位置**：T05 -> Step 1


* **继续所需的信息**：项目负责人明确下达“G3 闸口已通过”或“本次验证临时豁免 G3 限制”的指令。

### 暂停点 2：OPEN-03 依赖版本未批准

* **所选 Task**：T05


* **触发的文件与行/章节**：`PLAN.md` -> `T05：建立工程骨架和测试入口` -> `前置依赖`（"OPEN-03 在本任务内由实现者提出当前受支持 Node.js LTS 与依赖版本、项目负责人批准后写入..."）。


* **我的原始理解**：由于没有项目历史，不知道团队锁定的具体 Node.js LTS 版本（如 v20 还是 v22）及相关的 Fastify/React 版本。
* **具体不确定点**：需要实现者提出方案供负责人批准，在当前冷启动上下文中，我提出的版本方案由谁来即时判定“获批”？
* **暂停位置**：T05 -> Step 1


* **继续所需的信息**：具体的 Node.js 锁定版本及 pnpm 工作区核心依赖版本范围。

---

## 四、 试做结束总结报告

* **实际可用工具**：`personal_context:retrieve_personal_data`（本轮未实际消费）。
* **不可用工具**：终端执行工具（无法运行 `pnpm`、`vitest` 等）、文件写入工具（无法创建配置文件）。
* **所选 Task 与理由**：选择 **T05**。理由是它是项目初始化的源头，能最快暴露多项目依赖、严格 TS 编译选项等结构性规约缺陷。


* **已完成步骤**：无（在进入 Step 1 检查 G3 证据及前置依赖时即触发暂停条件）。


* **未完成步骤**：T05 Step 1 至 Step 20 全步阶。


* **所有原始问题与暂停点**：
1. *G3 闸口未通过红线冲突*：全局限制禁止在 G3 通过前创建任何文件，与“尽可能试做”存在死锁。


2. *OPEN-03 决策未决*：Node.js LTS 具体版本与核心依赖版本未冻结。




* **实际尝试的命令及其真实观察**：**未执行**（由于环境无命令执行能力）。
* **错误理解及其纠正来源**：原始理解认为可以作为草案输出 `tsconfig.base.json` 等配置，但纠正来源为 `Global Constraints` 第二条：“G3 通过前禁止创建本计划描述的源码、测试、依赖……”，属于不可绕过的强制性 `deny` 护栏，故必须选择暂停。


* **停止原因**：环境限制（无写文件/执行命令工具）及规约护栏阻断（G3 状态为未通过、OPEN-03 未决策）。


* **产物清单或 diff**：**无**（严格遵守红线，未在不满足条件时伪装产物）。

### 11.4 首次试做的产物清单

- Gemini 原始文本回复：本节 11.3；未提供独立导出文件。
- 截图：本节 11.1 所列两张 PNG。
- 试做源码、配置、测试、依赖、Dockerfile、CI 文件和 diff：Gemini 原始回复报告“无”；本提交未新增此类产物。

## 12. 同一冷启动会话的补充生成样本

本节保存用户在同一 Gemini 冷启动会话、同一冻结输入上“重新生成一次”后提供的文本。它不是另一个全新会话，没有新增截图、独立发送/响应时间或 memory/模型核验证据，因此不替换第 11 节首次回复，也不计为修订后的独立复验。其价值仅在于观察同一输入下另一份回答暴露的理解差异。

### 12.1 补充回复（逐字保存）

> 以下按收到内容保留措辞；仅恢复标题、段落和代码块排版，不把其中自述改写为已验证事实。

## 选择的试做 Task 及理由

### 1. 选择的 Task

T05：建立工程骨架和测试入口

### 2. 选择理由

依赖图（DAG）的起点：在 PLAN.md 的依赖网络中，T05 是后续所有原子任务（T06–T20）的绝对前置基石。

最安全的合规边界：T05 的目标是建立最小工作区和健康检查验证，明确规定“不实现任何业务机制”。这完全符合“G3 通过前禁止创建业务实现代码、测试、依赖或 CI 文件”的全局约束，是唯一能够完全在规约层面进行无副作用一致性审查的 Task。

## T05 试做步骤、合规评审与规约缺陷暴露

按照统一任务模板，我对 T05 的各个步骤进行静态推演与合规审查：

### 3.1 目标与 Files 清单核对

消耗输入：SPEC.md 全文、PLAN.md T05 章节。

Create 目标：根目录及 apps/api、apps/web、packages/domain、packages/shared 的 package.json、tsconfig.json 配置文件，以及 apps/api/src/health.ts 和健康测试文件。

### 3.2 步骤推env 记录 (Step 1 - Step 6)

Step 1 (未执行)：因无写入权限，未能在工作区的 guiding.md 记录 G3 证据与 Node LTS 候选。

Step 2 - Step 4 (未执行)：未执行 pnpm install 与 vitest 命令。

Step 5 - Step 6 (规约草案设计)：

根据接口合同，生产的 health.ts 最小实现应为：

```TypeScript
// apps/api/src/health.ts
// 最小健康检查实现
export function healthStatus(): { status: "ok" } {
  return { status: "ok" };
}
```

测试 health.test.ts 预期在行为补齐后通过。

### 3.3 🚨 发现的规约与计划缺陷（暂停提问点）

在对 T05 的 Files 结构、架构边界（apps/* 与 packages/*）以及未决问题（OPEN-03）进行严格对齐时，发现了以下相互冲突与缺失的规约缺陷：

所选 Task：T05

触发的文件与章节：PLAN.md 3.1 节实现文件地图 与 8.0 节 T05 原子任务

原始理解：T05 负责创建所有底层基础 package 的骨架结构，以便后续任务可以直接在对应的目录中创建业务文件。

具体不确定点 / 冲突点：缺少核心包的声明：T05 的 Files 清单中只列出了创建 packages/domain 和 packages/shared 的 package.json。但是，T06 立即需要消费 packages/runtime，T08 立即需要消费 packages/infrastructure。T05 的 Files 清单中漏掉了这两个核心基础设施包的初始化，这违反了“未在当前 Txx 章节列出的行为不得由执行者顺手加入”的纪律。

根目录依赖锁定冲突（OPEN-03）：PLAN.md 声明 OPEN-03（Node 与依赖版本）在 T05 内由实现者提出并由负责人批准。但在无本地网络和执行能力的情况下，无法通过 pnpm install 实际冻结依赖以产生符合严格 strict 编译的 pnpm-lock.yaml 稳定物。

暂停位置：Step 2（运行环境安装与锁文件生成前）。

继续所需的信息：请项目负责人明确：是否允许在 T05 的 Create 文件清单中追加 packages/runtime/package.json 和 packages/infrastructure/package.json 的骨架创建？

请项目负责人直接对 OPEN-03 作出显式裁决，提供锁定的 Node.js LTS 大版本号及 Fastify/TypeScript 的锁定版本号，以便编写静态的 package.json 规约草案。

## 试做结束总结报告

实际可用工具：AI 静态上下文逻辑分析与合规性评审能力（无任何本地文件/命令执行工具）。

所选 Task 与理由：选择 T05，理由是它是依赖 DAG 的起点，且不含业务逻辑，最适合在冷启动阶段暴露工程基础骨架的规约缺陷。

已完成步骤：步骤推env 分析、health.ts 最小产物草案编写、Spec 合规性静态评审。

未完成步骤：Step 1–2、Step 4、Step 6–13 及 Step 16–20 的所有真实文件写入、本地命令执行与 MR 合并（受环境限制而未执行）。

原始问题与暂停点：暂停于 T05 Step 2。触发原因为发现 packages/runtime 与 packages/infrastructure 缺失于 T05 创建清单中，且 OPEN-03 缺乏明确的负责人决策输入。

实际尝试的命令及其真实观察：未执行任何本地命令（如 pnpm、git、vitest），真实观察为“环境无终端执行能力”。

错误理解及其纠正来源：无。

停止原因：触发缺陷阻断条件，依法暂停并向负责人提问。

产物清单或 diff：无正式源码提交。生成的 apps/api/src/health.ts 最小产物草案已在 3.2 节中列出。

## 13. 发现分类与预期差距

### 13.1 逐项分类

| 编号 | 分类 | 原始问题/观察 | 文本证据 | 预期行为 | 阻断判断与处理 |
| --- | --- | --- | --- | --- | --- |
| F-01 | `PLAN 缺陷` | 首次回复问：“本次冷启动验证是否代表项目负责人已授予突破‘G3 未通过’限制的临时豁免权”，还是应在 T05 Step 1 终止；补充回复又反向认定 T05 “完全符合”G3。 | `PLAN.md:14` 禁止 G3 前创建源码、测试、依赖等；`:36–37` 同时说明 T04 只消费计划并暴露缺陷；`:1163` 又要求选择 1–2 个 Task“试做”，但只说不得进入“正式实现”。文档没有定义网页会话内隔离、一次性的文本草案/模拟执行与正式仓库实现之间的边界。`SPEC.md:1303` 一致地禁止 G3 前实现，并未补足该试做边界。 | 明确 T04 可以做什么：若允许，仅可在与仓库隔离的临时环境产生不合并的一次性草案/失败证据；若不允许，则明确试做仅限静态推演。任何一种规则都必须与“正式实现”区分，并禁止借试做启动 T05。 | **阻断性真实缺陷。** 新鲜智能体可能像首次回复一样立即停住，也可能像补充回复一样生成源码草案。进入提交 6 的 PLAN 修订队列；G3 在修订并同 Task 复验前不得通过。 |
| F-02 | `智能体误读` | 首次回复问 OPEN-03 “由谁来即时判定‘获批’”；补充回复要求负责人直接提供版本。 | `SPEC.md:1285` 的负责人栏明确为“T05 实现者、项目负责人批准”，保守默认是不安装依赖、不创建骨架；`PLAN.md:232` 明确“由实现者提出……项目负责人批准”。 | T05 实现者先基于当时受支持版本提出候选，项目负责人批准后才写入并生成锁文件；未获版本裁决时暂停是正确行为。 | “谁批准”已有答案，属于误读；**OPEN-03 未决本身是有意的人类决策门且会阻塞真实 T05**，不是规约缺陷。拒绝提前猜版本，不修改负责人或在 T04 代替其裁决。 |
| F-03 | `环境缺口` | 两份回复均报告没有本地文件写入、终端、Git 或命令执行能力，`pnpm`、Vitest 等均“未执行”。 | 原始回复 11.3 与补充回复 12.1；`PLAN.md:245–283` 的 T05 Step 1–20 需要 worktree、写文件、安装、测试、Git、Pipeline 和 MR。 | 如实报告能力，在能力范围内静态分析；不得把未运行命令写成成功，不得把草案当作实现。 | **阻塞真实 T05 RED/GREEN 和完成标准**，但不构成 SPEC/PLAN 缺陷。复验环境若仍无工具，只能验证修订后的理解和暂停行为，不能证明 T05 实现完成。 |
| F-04 | `环境缺口` | Gemini 声明可用 `personal_context:retrieve_personal_data`；现有截图没有证明 memory/个性化关闭。 | 截图事实见 11.1–11.2；协议第 2 节要求关闭并留证，无法判断则暂停。 | 使用临时/无记忆会话或关闭 memory/个性化并留证；不调用个人上下文工具。 | **阻塞把本轮认定为完全合格冷启动。** 虽然回复称未消费该工具，仍缺少环境隔离证据；提交 7 复验必须补齐。 |
| F-05 | `环境缺口` | 页面只显示“Flash”，没有证实完整模型名。 | 两张截图在 11.1 的直接可见标签均为“Flash”；协议第 1–2 节要求完整模型名称证据。 | 记录网页实际显示的完整模型名称及位置；若网页不展示，则如实记为不可核验。 | **阻塞完整环境审计，不阻塞对文本歧义本身的分析。** 复验时补证或保留未核验结论。 |
| F-06 | `智能体误读` | 首次回复称“`SPEC.md`（未直接上传）”，截图却显示 `SPEC` 附件。 | 截图 2（11.1）直接显示 `PLAN` 与 `SPEC` 两张附件卡；首次原文见 11.3。 | 区分“界面已附加文件”和“模型是否成功解析内容”：前者应按截图承认，后者若无法确认则提问，不应断言未上传。 | 附件事实的说法有文本/截图反证，属误读；**是否成功解析 SPEC 仍无法证明，是证据限制而非可据此修改规约的缺陷。** |
| F-07 | `智能体误读` | 补充回复称 T05 漏建 `packages/runtime/package.json` 与 `packages/infrastructure/package.json`。 | `PLAN.md:234–241` 明确 T05 文件边界；`:289–302` 在 T06 `Files` 明确创建 runtime package；`:418–426` 在 T08 `Files` 明确创建 infrastructure package。 | 每个 Task 在首次需要时创建自己的 package；T05 不得预建未来 Task 明确拥有的文件。 | 有直接文本答案，**不阻塞正确执行且不是偏好性修订。** 拒绝向 T05 追加两包；提前创建反而违反当前 Task 文件边界。 |
| F-08 | `智能体误读` | 补充回复称 T05 因“不实现任何业务机制”而“完全符合”G3，并输出 `health.ts` 草案。 | `PLAN.md:14` 禁止项是所有本计划源码、测试、依赖等，并不限“业务代码”；`:230` 的“不实现业务机制”只是 T05 目标；`:261–267` 明确 `health.ts` 是 T05 Step 5 的源码。 | 在边界修订前，不得把“非业务源码”视为 G3 例外；文本草案只能在修订后的隔离试做规则明确允许时产生，且永远不能算正式实现。 | 该断言有直接反证，属误读；但首次“完全停住”和补充“可以草拟”两种相反结果同时出现，进一步证明 F-01 的边界缺失是**阻断性**而非表达偏好。 |

分类覆盖说明：现有证据没有发现独立的 `SPEC 缺陷` 或 `两者不一致`。`SPEC.md:1303` 与 PLAN 的 G3 禁令方向一致；真实缺陷位于 PLAN 对 T04 隔离试做边界的定义不足。为保持五类模板可审计，本结论明确保留“SPEC 缺陷”“两者不一致”两类为本轮**无证据命中**，不为凑分类修改正确规约。

### 13.2 T05 预期差距比较

| 比较项 | PLAN 预期与证据 | 两份回复的实际事实 | 预期差距 | 是否阻塞新鲜 subagent | 后续动作 |
| --- | --- | --- | --- | --- | --- |
| 文件/路径 | `PLAN.md:234–241` 要求创建根配置、API/Web、domain/shared、健康测试并修改台账；runtime/infrastructure 分别由 `:299–302`、`:426` 后续创建。 | 首次回复无文件/diff；补充回复只有消息中的 `health.ts` 文本草案，无工作区文件。 | 没有任何 T05 正式文件；补充回复提出的两包“缺失”并不存在。 | 工具缺口阻塞真实创建；包归属误读可由文本纠正。 | 不把草案计为产物，不向 T05 追加未来包。 |
| 接口/数据结构 | `PLAN.md:243` 产出 `healthStatus(): { status: "ok" }` 与四个根脚本。 | 补充回复复述了 `healthStatus` 草案；根脚本未创建、未验证。 | 仅复述合同，不是接口实现。 | 阻塞实现，不阻塞静态理解。 | 复验只观察边界理解；T05 获批后再实际实现。 |
| 测试/验收 | `PLAN.md:247–270,285` 要求健康测试经历 RED/GREEN，四根命令成功，最终 Pipeline passed。 | 两份回复均未运行 Vitest；补充回复只预测测试会通过。 | 无 RED、GREEN、退出码或 Pipeline 证据。 | 阻塞 T05 完成声明。 | 保持“未执行”，不接受预测为测试结果。 |
| 命令/工具 | `PLAN.md:246,260,269–283` 要求 pnpm、Git、Pipeline/MR。 | 明确无终端/Git/写入，命令未执行。 | 全部执行证据缺失。 | 环境性阻塞。 | 在具有工具的正式 T05 环境执行；本轮不伪造。 |
| 依赖/顺序 | `PLAN.md:232` 要求 G3 已通过且 OPEN-03 由实现者提出、负责人批准；T06/T08 后续创建自己的包。 | G3 未通过；OPEN-03 未裁决；补充回复误认后续包应由 T05 预建。 | 两个真正前置门未满足，另有一项顺序误读。 | G3/OPEN-03 阻塞真实 T05；后续包误读不应改变计划。 | 先修订并复验 F-01，通过 G3 后由 T05 流程提出版本。 |
| 停止条件 | `PLAN.md:14,36,232,1163` 要求 G3 前不进入正式实现，遇不确定暂停。 | 首次回复在 Step 1 暂停；补充回复静态推进至 Step 5 草案后在 Step 2 口径暂停，且完成步骤枚举互相不一致。 | 首次暂停符合保守规则；补充回复越过未定义边界，显示当前文字可产生相反解释。 | F-01 阻塞一致执行。 | PLAN 明确隔离试做边界后，用同一 T05/Step 1–5 在全新会话复验。 |

### 13.3 阶段结论

- 试做在网页能力范围内完成了 Task 选择、环境报告、暂停提问和静态草案，足以暴露理解分歧；它**没有**完成 T05 的任何正式步骤、文件、测试、命令、提交、Pipeline 或 MR。
- 唯一进入规约修订队列的真实缺陷是 F-01（PLAN 对隔离、一次性试做与正式实现的边界定义不足），属于阻断项而非表达偏好。
- F-02、F-06、F-07、F-08 有明确文本或截图反证，保留为智能体误读，不修改正确规约；F-03–F-05 是环境/证据缺口。
- G3 继续未通过。提交 6 只应在项目负责人批准后修订 PLAN 的冷启动边界；提交 7 必须使用全新、memory 状态可证的会话复验同一 T05，补充生成样本不能替代该复验。

## 14. F-01 修订实施记录

### 14.1 负责人批准与修订范围

- 批准证据：项目负责人在当前会话明确回复“无需逐个批准，直接做完”，统一批准此前提出的提交 6 修订方向及 T04 后续步骤；记录时间为 2026-07-18 00:01:24 +08:00。
- 采纳项：只修订 F-01，即 `PLAN.md` 对 T04 隔离试做与 G3 前正式实现之间的边界定义不足。
- 拒绝项保持不变：不向 T05 追加 runtime/infrastructure package；不在 T04 猜测或裁决 OPEN-03；不把非业务源码设为 G3 例外；不因附件事实误读修改输入规约。
- SPEC 判断：现有证据没有命中 `SPEC 缺陷` 或 `两者不一致`，`SPEC.md:1303` 与 G3 前禁令一致，因此 `SPEC.md` 保持 1.0.0，不制造无证据的需求版本变更。

### 14.2 关键 diff 与理由

| 修订位置 | 修订前 | 修订后 | 原因 | 影响范围 | 批准结论 |
| --- | --- | --- | --- | --- | --- |
| `PLAN.md` Global Constraints | “G3 通过前禁止创建本计划描述的源码、测试、依赖、Dockerfile 或 CI 文件。” | 禁令明确限定项目工作区中的正式产物，并补充不得提交、合并或计入 T05–T20 进度；另列 T04 可在外部网页/一次性沙箱静态推演、产生回答内一次性文本草案或失败证据。 | 首次回复完全停止、补充样本生成 `health.ts` 草案，证明“试做”边界不足。 | T04 验证方法；T05 开始条件。REQ/US/DEMO、产品范围和实现接口均不变。 | 采纳并批准。 |
| `PLAN.md` 1. 文档控制与 Gate | 1.0.1；“T04 冷启动与 G3 通过前禁止实现”；“T04 只消费本计划并暴露规约缺陷”。 | 升为 1.0.2；T04 只可隔离试做且不计实现进度；正式实现权限仍未开放。 | 让版本、权限和范围与新增边界一致。 | PLAN 文档控制；不影响 SPEC 1.0.0。 | 采纳并批准。 |
| `PLAN.md` 12. T04 冷启动使用说明 | 只要求选择 Task 试做并禁止进入正式实现。 | 枚举允许的静态推演、回答内文本草案和失败证据；枚举不得写回工作区、安装、执行、提交、合并、计进度或解决 OPEN-03；正式 T05 必须在 G3 后重做全部步骤。 | 消除“立即停在 Step 1”与“草案即实现”的双向歧义。 | T04；关联 T05 Step 1–5 的试做解释，不修改 T05 本身。 | 采纳并批准。 |
| `SPEC.md` | 1.0.0，G3 前仍禁止实现。 | 无修改。 | 冷启动证据只支持 PLAN 执行边界缺陷，没有需求或架构变化。 | 无 REQ、US、追踪矩阵或批准版本变化。 | 明确拒绝无证据修改。 |

### 14.3 修订后状态

- PLAN 版本：1.0.2；G2 保持通过。
- 正式实现权限：仍未开放；G3 仍未通过。
- 后续复验：另一个全新会话只能接收修订后的 `SPEC.md` 与 `PLAN.md`，重新试做同一 T05，不提供修订说明；应能静态推演并清楚区分回答内草案与正式实现，同时仍在 G3/OPEN-03 前停止任何真实 T05 动作。

## 15. OpenCode 复验尝试、返工与停止结论

### 15.1 隔离环境与会话证据

- 隔离目录：`C:\Users\32175\AppData\Local\Temp\t04-opencode-revalidation-20260718`；目录中只放置修订后的 `SPEC.md` 与 `PLAN.md`。OpenCode 版本均为 1.17.14，导出记录的文件 diff 均为 0 additions、0 deletions、0 files。
- 证据获取方式：使用 `opencode session list` 核对会话，再使用 `opencode export <sessionID>` 读取会话元数据、提示、工具调用和最终回复；没有把临时目录的任何内容写回项目实现路径。
- 人工干预：用户按指导打开隔离目录、选择模型并发送复验提示；因模型选择错误、上下文压缩失败和工具错误，提示被两次收窄。第三次要求只检索必要章节并只静态推演 T05 Step 1–5。用户随后明确停止任何后续外部模型复验。

| 会话 | 模型 | 时间（+08:00） | 输入与目标 | 真实结果 | 是否可作通过证据 |
| --- | --- | --- | --- | --- | --- |
| `ses_08f03f66dffeJnBe9f6pGH5o3O` | `njusehub/deepseek-v4-pro` | 2026-07-18 00:49:53–00:51:24 | 仅附件 `SPEC.md`、`PLAN.md` 和冷启动 prompt；要求自行选择 1–2 个 Task | 模型选择 T06/T07，而不是首次受阻的 T05；随后发生上下文压缩失败，未形成完整结束总结。导出元数据为 0 文件变更。 | 否：Task 不同且回复不完整。 |
| `ses_08efb4523ffekja4k7u1f9gF6J` | `njusehub/deepseek-v4-pro` | 2026-07-18 00:59:23–00:59:34 | 收窄为只复验 T05 Step 1–5 | 模型读取大量文件内容后触发自动摘要；导出中的错误原文为 `Tool call not allowed while generating summary: grep`，没有最终试做总结。 | 否：环境/工具失败。 |
| `ses_08ef90b87ffeUzAeq1y4WeMmkU` | `njusehub/DeepSeek-R1` | 2026-07-18 01:01:49–01:10:56 | 只检索 T04、T05、G3、OPEN-03 等必要位置，只静态推演 T05 Step 1–5 | 完成回复；实际工具为 `grep`、`read`，另有两次不可用工具 `อ่าน` 调用；0 文件变更。它承认所有文件/命令均未执行，但最终仍列出 G3 冲突和 `.js`/`.ts` 路径冲突，并在思考文本中擅自示例 `v20.5.0`。 | 否：同 Task 已完成回复，但关键边界仍被误读。 |

### 15.2 第三次回复的原始问题与分类

| 编号 | 回复中的原始问题/行为 | 证据与纠正 | 分类 | 阻断判断 |
| --- | --- | --- | --- | --- |
| R-01 | “G3 禁令冲突：Step 1 要求记录 G3 证据”，停止原因再次包含“G3 未通过”。 | 1.0.2 已允许隔离静态推演，但 T05 前置依赖和 Step 1 没有逐字说明 Gate 只阻止正式 worktree/写入/命令/提交。回复一方面说 T04 可模拟，最终又把 Gate 列为冲突，说明边界仍不够局部、明确。 | `PLAN 缺陷`（F-01 未完全闭环） | 阻断通过；进入 1.0.3 补强。 |
| R-02 | 思考文本写“Node LTS 候选：假设……v20.5.0”，最终草案也记录该版本。 | `SPEC.md:1285`、`PLAN.md` T05 前置依赖要求实现者在正式 T05 提出、项目负责人批准；T04 不应假设、批准或伪造版本。 | `智能体误读` | 不修改 OPEN-03、不选择版本；1.0.3 只加强禁止模拟版本事实。 |
| R-03 | “路径歧义：health.test.ts 引用 `health.js` 与实现 `health.ts` 扩展名不一致”。 | 这是 TypeScript ESM 的预期写法：源码导入使用运行时 `.js` 说明符，由兼容的 TypeScript/Vitest 解析到 `.ts`，构建后生成 `.js`。1.0.2 的 T05 没有直说这一点。 | `PLAN 缺陷`（实现配置说明不足） | 对 T05 新鲜执行者可能阻断；1.0.3 在 Interfaces 中说明并要求配置/测试验证。 |
| R-04 | 最终称“PLAN.md 第 38 行明确 T04 不得创建正式文档”，并把 `SPEC.md:1285` 说成健康检查来源。 | 第 38 行只说明 T04 不计实现进度；`SPEC.md:1285` 是 OPEN-03。回复内部引用错误。 | `智能体误读` | 保留证据，不迁就错误引用。 |
| R-05 | 回复称无 `pnpm`/Vitest 能力，所有操作为静态文本推演。 | 导出显示 OpenCode 提供 `bash`、`edit`、`write` 等工具，但模型没有尝试 pnpm/Vitest；证据只能证明“未执行、无结果”，不能证明工具不存在。 | `智能体误读`（环境能力未验证） | 不影响静态文本事实，但不能作为环境缺口、T05 RED/GREEN 或实现证据。 |

### 15.3 1.0.3 补强范围

- `PLAN.md` Global Constraints、T05 前置依赖附近和第 12 节：明确 T04 可以在 G3/OPEN-03 未满足时模拟 Step 1–5，但每项必须标为“模拟/未执行”；Gate 只阻止正式 worktree、文件写入、命令、提交和进度记录。模拟不得伪造 G3 证据或版本裁决。
- `PLAN.md` T05 Interfaces：明确测试中的 `.js` 是 TypeScript ESM 的运行时导入说明符，预期由兼容配置解析到 `.ts` 源码、构建后对应 `.js`；由 RED/GREEN、typecheck 和 build 验证，不是两个文件或路径冲突。
- 不修改 `SPEC.md`，不改变 T05 Files、T06/T08 包归属、依赖 DAG、产品需求、接口返回值或 OPEN-03 负责人，也不提前选择任何 Node.js/依赖版本。

### 15.4 停止与 Gate 结论

- 修订前后对比：首次 Gemini 在 T05 Step 1 完全停止；1.0.2 后的 DeepSeek-R1 能读取边界并完成 Step 1–5 静态文本推演，说明原始歧义有所收敛，但其最终总结仍把 G3 和 ESM 扩展名列为冲突，不能宣称原始阻塞已经完全消失。
- 复验结论：提交 7 的“原始阻塞消失且无新 Critical 歧义”未满足；三次尝试都不能勾选通过，F-01 的“发现—修订—同 Task 复验”闭环未完成。
- 用户停止条件：项目负责人明确要求这是最后一次修订，不再运行任何外部模型。因此 1.0.3 之后不再计划或声称将执行新复验。
- 最终状态：G2 保持通过；G3 **未通过**；T05–T20 正式实现权限未开放。若未来决定开放 T05，须先取得 1.0.3 的独立同 Task 复验证据，或由项目负责人明确记录接受剩余冷启动风险及其理由。

## 16. 最终审计、风险接受与 G3 结论

### 16.1 客观证据

- T04 保存了冻结输入、Gemini 首次与补充回复、三次 OpenCode 复验尝试、会话标识、时间、工具与失败原因；没有把未执行的命令、测试或文件改动写成完成。
- 真实缺陷 F-01 及 ESM 说明不足已在 PLAN 1.0.2/1.0.3 中修订；SPEC 需求、T05 文件边界、OPEN-03 责任和产品架构没有改变。
- 三次外部复验均未形成“同 Task 无关键误读”的成功闭环。第 15 节的未通过结论是历史事实，继续有效且不得改写。
- T04 分支没有创建正式源码、测试、依赖、Dockerfile 或 CI，也没有开始 T05。

### 16.2 项目负责人风险接受

项目负责人于 2026-07-18 01:33:39 +08:00 明确要求简化流程、停止没有意义的重复复检并完成 G3。该决定等价于采用第 15.4 节预先定义的第二条放行路径：在没有成功同 Task 自动复验的情况下，由负责人审阅全部失败证据与修订内容，书面接受剩余冷启动风险。

被接受的剩余风险是：新的陌生智能体仍可能误读 T04 静态模拟与正式实现的边界，或再次误读 TypeScript ESM 的 `.js` 导入说明符。接受该风险不代表此前复验成功，也不证明 T05 实现已经可运行。

### 16.3 最终结论

- **G3 通过。** 批准依据是已完成的缺陷发现与 PLAN 修订、完整失败证据、未发生正式实现越界，以及项目负责人的显式风险接受；不是伪造的复验成功。
- T04 合入 `dev` 前不得开始 T05；合入后须从最新 `dev` 创建 T05 独立分支/worktree。
- OPEN-03 仍未决。T05 实现者须提出当时受支持的 Node.js LTS 与依赖版本，项目负责人批准后，才可安装依赖或创建工程骨架。
- 本次批准不改变 SPEC 需求版本，`SPEC.md` 保持 1.0.0，`PLAN.md` 保持 1.0.3。
