# AI4SE Coding Agent Harness 规约形成过程

## 文档用途

本文件记录 T01 brainstorming 中的问题、角色化访谈推演、AI 建议、项目负责人判断和设计变化，为 T02 编写正式 `SPEC.md` 提供可追溯的设计输入。模拟目标用户的回答必须明确标注，不作为真实外部用户调研数据；未经项目负责人确认的内容不得写成最终结论。项目负责人的口语化表达可以在不改变原意的前提下整理，并标注为“整理后的表述”。

## 记录模板

### 关键迭代 N：主题

- 问题：
- 参与者回答（真实或模拟必须标注）：
- AI 建议：
- 本人判断：
- 设计变化：

## 初始设想

- 项目方向已确定为 A：Coding Agent Harness。
- 项目应自研 Harness 内核，而不是用现成 Agent Runner、提示词、Skill 或配置文件冒充核心机制。
- 六个维度都需要可运行的最低代码实现：决策、工具、记忆、治理、反馈、配置。
- 核心机制必须能在移除真实 LLM 后，由 mock / stub LLM 驱动确定性测试。
- 当前尚未确认具体产品、目标用户、结构性问题、能力边界和主要贡献维度。

## 已知硬约束

- T01 只允许需求探索与文档记录，不创建 `SPEC.md`，不编写实现代码。
- brainstorming 必须逐问逐答，至少比较三个真实可行方案。
- 必须形成陌生人可在 30 秒内理解的价值陈述，并明确为什么不直接使用现成 Coding Agent。
- 必须明确非目标，排除万能 Agent 和无边界自主执行。
- 必须为 Harness 六个维度形成最低实现边界与确定性验证思路。
- 必须选择一个机制密集的主要贡献维度，并记录采纳、拒绝或修改理由。

## 待回答问题

以下仅为待讨论主题，不代表已经确认的结论：

1. 目标用户是谁，其当前工作方式是什么？
2. 用户最频繁、代价最高且适合由 Coding Agent Harness 改善的问题是什么？
3. 至少三个具体使用场景的触发、输入、期望结果和失败影响是什么？
4. 为什么现成 Coding Agent 不能充分解决该问题？
5. 项目的明确非目标是什么？
6. Agent 可以做什么、禁止做什么？
7. 工具、反馈、治理、记忆、主循环、配置与可观测性的最低机制是什么？
8. 治理、反馈闭环、运行控制、记忆与上下文等候选主要贡献方向中，应选择哪一个？

## Brainstorming 记录

### 关键迭代 1：模拟目标用户访谈——从泛化 Harness 到团队决策连续性

- 访谈方式：AI 扮演小型项目开发团队成员，围绕目标用户、工作方式、痛点、代价和使用场景连续回答问题；项目负责人在访谈结束后审阅并确认设计结论。
- 真实性说明：下列“模拟用户回答”是角色化需求推演，不代表已完成真实外部用户访谈。

- 问题：这个 Coding Agent Harness 的首要目标用户是谁？
- 模拟用户回答：小型项目开发团队。
- AI 建议：先把“小型项目开发团队”作为唯一的主要用户群体，再继续确认团队规模、当前 Coding Agent 协作方式和最突出的问题，避免同时面向个人开发者与高合规大型团队而扩大范围。
- 追问：团队当前最接近哪种工作方式？
- 模拟用户回答：每个成员各自使用 Codex、Claude Code 等 Coding Agent，最后通过 Git / MR / CI 汇合。
- 追问：这种协作方式目前最严重的问题是什么？
- 模拟用户回答：需求、历史决策和上下文分散在不同成员的会话中，后续 Agent 容易误解或重复劳动。
- 追问：上下文与历史决策分散后，团队付出的最大代价是什么？
- 模拟用户回答：新改动违背旧设计约束，合并时才发现冲突。
- 追问：这个问题是实际高频问题还是项目假设？
- 模拟用户回答：在该用户画像下，预计每个迭代会遇到一至两次；该数字是假设，后续需要通过真实试用校准。
- 本人判断：形成“上下文分散 → Agent 不知道既有设计约束 → 新改动偏离约束 → MR 合并阶段才暴露”的首要问题链；发生频率暂按产品假设处理，不把模拟回答写成实测数据。
- 设计变化：产品价值不再泛指“共享上下文”，而是要让后续 Agent 在行动前获得可追溯的有效设计约束，尽早阻止违背旧决策的改动。

#### 使用场景 1：依赖或配置变更

- 触发：团队成员让 Agent 升级依赖或调整项目配置。
- 输入：本次依赖或配置变更目标、相关项目文件，以及团队既有的配置文件格式和环境变量接口约束。
- 期望结果：Agent 在改动前获得相关约束；若拟议改动会改变配置文件格式或环境变量接口，Harness 必须暂停执行，展示发生冲突的历史约束及其来源，并请求人工批准。
- 失败影响：Agent 擅自改变已有配置文件格式或环境变量接口，直到 MR 合并阶段才暴露并引发返工，也可能破坏现有部署或调用方。
- 状态：模拟访谈已形成完整场景，项目负责人已审阅确认。

#### 使用场景 2：并行开发中的决策版本漂移

- 触发：不同成员并行开发，分别让自己的 Agent 执行不同任务。
- 输入：两个并行任务、各自工作分支，以及各 Agent 获得的项目决策版本。
- 期望结果：Harness 识别 Agent 使用的项目决策版本是否过期；发现过期时暂停任务，展示决策差异，刷新上下文，并要求 Agent 根据最新决策重新规划后再继续。
- 失败影响：两个 Agent 基于不同版本的项目决策产生互相冲突的改动，直到 MR 汇合时才发现，导致返工或错误合并。
- 状态：模拟访谈已形成完整场景，项目负责人已审阅确认。

#### 使用场景 3：新成员或新 Agent 接手任务

- 触发：新成员加入项目，或团队为新任务启动一个没有历史会话的新 Agent。
- 输入：任务目标、当前代码库状态、带版本和来源的团队决策记录。
- 期望结果：Harness 只选择与当前任务相关且仍然有效的决策，生成精简上下文包；每条约束保留来源、版本和适用范围。
- 失败影响：新成员或新 Agent 需要翻阅全部聊天记录，仍可能遗漏隐含约束、重复解释背景或按错误假设实现功能。
- 状态：模拟访谈已形成完整场景，项目负责人已审阅确认。

### 项目负责人审阅结论：产品价值与边界

#### 产品定位

为成员分别使用 Coding Agent、最终通过 Git / MR / CI 汇合的小型项目开发团队，提供一个“决策感知型 Coding Agent Harness”。现有 Coding Agent 继续负责代码生成；本 Harness 将散落在不同会话中的有效设计决策转化为带版本、来源和适用范围的约束，并在 Agent 行动前按任务检索、检查和治理。

#### 30 秒价值陈述

面向成员分别使用 Coding Agent 的小型开发团队，本项目提供一个可追溯、版本化的团队决策 Harness。它在 Agent 编码前提供当前任务真正需要的设计约束，并在发现过期决策或契约冲突时暂停执行、请求人工确认，从而把原本在 MR 阶段才暴露的设计冲突提前到行动之前。

#### 为什么不直接使用现成 Coding Agent

- 现成 Coding Agent 负责生成代码，本项目不重复实现其代码能力。
- 结构性缺口是团队自己的跨成员、跨会话决策连续性。
- 普通规则文件较静态，难以表达决策版本、来源、适用范围和失效状态。
- 本项目用自研代码提供确定性的版本检查、契约冲突检测、HITL 和审计链。

#### 非目标

- 不做万能 Coding Agent，不训练自己的模型。
- 不替代 Git、MR、CI 或现有 Coding Agent。
- 不允许无边界、无人监督的自主执行。
- 不保存全部聊天记录，只保存对未来任务有结构性价值的决策。
- 首版不处理大型企业、多租户和强合规场景。
- 不把 prompt、Skill、规则文件或现成 Agent Runner 算作 Harness 内核。

#### 负责人确认

- 批准时间：2026-07-16 12:00:56 +08:00
- 负责人确认（整理后的表述）：认可上述产品价值、使用场景和范围边界，并继续完善 Harness 六维机制设计。
- 结论：项目负责人批准角色化访谈产出的产品价值、三个使用场景、现成 Agent 的结构性缺口和非目标，将其转为 T02 可使用的确认设计输入。

### 关键迭代 2：模拟机制访谈——从“共享上下文”到可执行的决策治理

- 访谈方式：AI 继续扮演目标团队成员，回答 Agent 能力边界、六维最低机制、安全策略和验证方式；下列回答属于角色化需求推演。
- 问题：Agent 最低限度应该完成哪些任务？
- 模拟用户回答：读取任务相关代码和有效决策，在工作区内修改文件，运行测试、lint、类型检查和构建，提出新的决策建议，并在验证通过后给出结果摘要。
- 问题：哪些事情明确禁止自动完成？
- 模拟用户回答：不得修改或删除已生效决策，不得绕过审批，不得读取凭据，不得访问工作区外路径，不得危险删除、联网部署或强推，不得把未验证结果声明为完成。
- AI 建议：把“决策连续性”落实为结构化 `DecisionRecord`、任务级 `ContextSnapshot` 和确定性的版本、契约检查；LLM 只提出动作，不负责判定动作是否安全或合规。
- 本人判断：接受该边界。普通聊天摘要、prompt 或规则文件只能提供信息，不能代替自研主循环、工具分发、冲突检测、审批状态机和反馈回灌。

#### 决策主循环

1. 接收任务目标、代码库状态和预算。
2. 按文件路径、模块和标签检索有效 `DecisionRecord`，生成带摘要值的 `ContextSnapshot`。
3. 通过可注入的 LLM 抽象层进行一次决策，输出结构化 Action。
4. 解析并校验 Action；未知动作或参数错误转换为结构化 Observation。
5. 检查上下文版本、路径边界、契约冲突和权限策略。
6. `ask` 动作暂停并等待 HITL；审批前工具不得执行。
7. 分发工具，记录 ToolResult，再运行反馈传感器。
8. 把工具结果、冲突或验证结果回灌下一轮上下文。
9. 仅在验证通过且不存在未处理冲突时完成；最大步数、预算耗尽、连续失败、审批拒绝、人工取消或环境故障均可停机。

#### 动作与工具

| 工具 | 输入 | 输出 | 边界与失败原则 |
| --- | --- | --- | --- |
| `decision.query` | 任务、路径、模块、标签 | 有效决策及 ID、版本、来源 | 只读；不得返回已失效决策作为当前约束 |
| `decision.propose` | 候选决策、理由、适用范围 | `proposed` 状态记录 | 不能自动转为 `active` |
| `file.read` | 工作区内相对路径 | 文件内容或结构化错误 | 解析真实路径并拒绝路径逃逸；敏感文件拒绝读取 |
| `file.write` | 相对路径、预期旧摘要、新内容 | 写入结果与新摘要 | 仅限工作区；写前检查快照和决策冲突 |
| `command.run` | 白名单命令、参数、超时 | 退出码、截断且脱敏的输出 | 不经 shell 拼接；限制目录、时间和输出长度 |
| `sensor.run` | 传感器名称、目标 | 结构化反馈 | 环境错误不得伪装为业务失败或通过 |

统一 `ToolResult` 至少包含 `status`、`data`、`error_code`、`redacted_output` 和 `evidence`。工具异常必须返回给主循环，不得吞掉错误或显示假成功。

#### 客观反馈

- `DecisionVersionSensor`：比较任务快照与当前有效决策版本，发现过期即返回 `CONFLICT`。
- `ContractDiffSensor`：比较配置格式和环境变量接口快照，检测删除、改名或不兼容类型变化。
- `CommandSensor`：运行测试、lint、类型检查和构建，保存退出码与摘要证据。
- 统一分类：`PASS`、`FAIL`、`CONFLICT`、`ENV_ERROR`、`TIMEOUT`。
- `FAIL` 或 `CONFLICT` 会进入下一轮上下文并要求重新规划；连续失败达到阈值后升级给人。

#### 危险动作与 HITL

- `allow`：读取普通项目文件、查询决策、运行 `git status` / `git diff` 和白名单验证命令。
- `ask`：改变配置格式或环境变量接口、激活或替代决策、修改共享核心文件、执行白名单外但可解释的非破坏性命令。
- `deny`：读取密钥或 `.env`、访问工作区外路径、危险删除、联网部署、强推、绕过验证或审批。
- 审批绑定动作类型、规范化参数、目标文件摘要、`ContextSnapshot` 摘要和有效期；任何内容变化都会使批准失效，批准只能消费一次。

#### 记忆与上下文

- `DecisionRecord` 字段：ID、标题、版本、状态（`proposed` / `active` / `superseded`）、适用路径或模块、结构化约束、设计理由、来源 commit / MR / 人工确认、创建时间和替代关系。
- `ContextSnapshot` 字段：任务 ID、代码状态摘要、选中决策的 ID / 版本集合、选择理由和整体摘要。
- MVP 使用路径、模块和标签做确定性选择，不引入向量数据库；新鲜 Agent 可以仅凭任务快照重建所需上下文。
- 任务结束清除临时消息和工具原始输出；长期保留有效决策和审计事件。
- API Key、token、密码、`.env` 内容、完整命令原始输出永不进入记忆。

#### 配置与可观测性

- 配置包含工作区、工具白名单、策略规则、传感器、最大步数、连续失败阈值、超时和输出上限。
- 配置必须通过 schema 校验；错误配置快速失败，默认策略采用最小权限。
- Trace 逐轮记录快照摘要、使用的决策 ID / 版本、LLM 动作、策略判定、审批状态、工具结果、传感器结果和停机原因。
- 日志自动脱敏并限制输出长度；Trace 可供单元测试、WebUI 和审计读取。

#### 六维最低代码机制与 mock 验证

| 维度 | 最低代码机制 | 不依赖真实 LLM 的验证 |
| --- | --- | --- |
| 决策 | 自研循环、Action 解析、预算与停机 | 脚本化 mock 依次返回动作，断言轮次和停机原因 |
| 工具 | 注册表、参数校验、受限文件与命令分发 | mock 工具记录调用，断言未知工具、越界路径和异常返回 |
| 记忆 | 决策记录、确定性选择、快照与清除 | 构造相关、无关和失效决策，断言只选择有效相关项 |
| 治理 | `allow` / `ask` / `deny`、审批状态机和动作摘要绑定 | 危险动作进入审批且工具调用为零；篡改参数后批准失效 |
| 反馈 | 版本、契约和命令传感器，结果回灌 | 第一次失败后 mock 改变动作，第二次验证通过 |
| 配置 | schema、安全默认值、脱敏 Trace | 错误配置快速失败，日志中不出现构造的假密钥 |

#### 本轮设计修订

- 修改前：用共享文档或聊天摘要保存“团队上下文”，由 LLM 自行理解是否冲突。
- 暴露的问题：内容缺少版本、来源和适用范围；冲突判断依赖 LLM，无法稳定复现和单测。
- 修改后：使用结构化决策记录、版本化任务快照和确定性传感器；LLM 负责提议，代码负责版本检查、契约检查、权限和停机。
- 结论：六个维度均有可运行的最低代码机制，且可以用 mock LLM 离线验证；本轮仍未选择主要贡献维度。

### 关键迭代 3：模拟方案评审——选择主要贡献维度

- 评审方式：AI 同时扮演目标团队代表与技术评审者，对三个可行方向进行角色化方案比较；项目负责人将在 T01 最终审计前监督结论。
- 共同前提：无论选择哪一项作为主要贡献，其余五个 Harness 维度仍须具有可运行、可测试的最低实现。

#### 方案一：治理与 HITL 深化

主要代码特性：

1. 可组合策略引擎：按动作、路径、决策类型和风险生成 `allow` / `ask` / `deny`。
2. 完整审批状态机：`pending`、`approved`、`denied`、`expired`、`consumed`。
3. 防 TOCTOU 的动作绑定：动作摘要、参数、文件摘要和上下文快照变化即使批准失效。
4. 可回放审计链：记录触发规则、审批人、理由、结果和时间。

评价：

- 用户价值：高，能直接阻断契约冲突后的危险执行。
- 机制深度：高，状态机与不变量明确。
- 确定性可测性：很高，无需 LLM 即可构造动作与审批事件。
- 实现风险：中等，难点在策略冲突和审批恢复。
- 演示效果：很高，危险动作被拦截直观。
- 项目周期：可控，但容易把产品重心变成通用安全网关。

#### 方案二：反馈闭环深化

主要代码特性：

1. 可扩展传感器注册表：决策版本、契约差异、测试、lint、类型检查和构建统一接入。
2. 失败分类器：区分 `FAIL`、`CONFLICT`、`ENV_ERROR`、`TIMEOUT`，决定重试、重规划或升级。
3. 多轮修正控制：将反馈回灌、限制连续失败、检测重复动作并升级给人。
4. 证据聚合：为每轮验证保存命令、退出码、摘要和关联动作。

评价：

- 用户价值：中高，能减少 Agent “假完成”和无效重试。
- 机制深度：高，适合形成通用反馈框架。
- 确定性可测性：很高，mock 传感器可复现失败与修复。
- 实现风险：中等，外部命令差异和环境错误分类较复杂。
- 演示效果：高，第一次失败、第二次修正通过的闭环清晰。
- 项目周期：可控，但与许多 Coding Agent Harness 的常规反馈能力相似，差异化较弱。

#### 方案三：版本化决策记忆与上下文深化

主要代码特性：

1. 版本化 `DecisionStore`：状态转换、`supersedes` 关系、乐观并发控制和不可变历史。
2. 范围感知选择器：依据路径、模块、标签和状态确定性选择决策，给出选择理由并排除无关或失效项。
3. 可复现 `ContextSnapshot`：记录代码状态、决策版本集合和摘要，同一输入生成同一上下文包。
4. 过期检测与重基线协议：比较任务快照与当前决策，展示差异，强制刷新上下文并重新规划。
5. 决策冲突检测：对相同范围内互斥约束生成结构化冲突，不让 LLM 静默选择一方。

评价：

- 用户价值：最高，直接命中“跨成员、跨会话决策断裂”的首要问题。
- 机制深度：高，包含版本模型、选择算法、快照和冲突协议。
- 确定性可测性：高，可用构造的决策历史和任务路径离线验证。
- 实现风险：中高，需要谨慎设计决策状态、范围匹配和冲突规则。
- 演示效果：高，可直观展示旧快照被拒绝、差异展示和新 Agent 获得精简上下文。
- 项目周期：可控，但必须坚持确定性规则，MVP 不引入向量检索和复杂知识图谱。

#### 方案比较结论

| 维度 | 治理与 HITL | 反馈闭环 | 版本化决策记忆与上下文 |
| --- | --- | --- | --- |
| 用户价值 | 高 | 中高 | 最高 |
| 机制深度 | 高 | 高 | 高 |
| 确定性可测性 | 很高 | 很高 | 高 |
| 实现风险 | 中 | 中 | 中高 |
| 演示效果 | 很高 | 高 | 高 |
| 周期适配 | 可控 | 可控 | 可控，需限制范围 |

#### 最终选择

- 主要贡献维度：**版本化决策记忆与上下文**。
- 选择理由：它与目标用户的首要问题和三个场景直接一致；若选择治理或反馈作为主角，产品会退化为通用安全或质量 Harness，无法解释为什么专门服务“小型团队的决策连续性”。
- 治理保留为基础实现：提供冲突后的 HITL、动作绑定和最小审计，不扩展成通用策略语言。
- 反馈保留为基础实现：提供版本、契约和命令传感器，以及失败回灌和升级，不构建复杂自适应评分系统。
- 拒绝方案一作为主贡献：虽演示直观，但不是本项目最独特的用户价值；治理用于执行决策约束，而不是产品目的本身。
- 拒绝方案二作为主贡献：虽最容易形成稳定闭环，但与现有 Coding Agent 的测试反馈机制差异不足。

#### 主要贡献成功判据

1. 相同任务、代码状态和决策版本集合生成完全相同的 `ContextSnapshot`。
2. 选择器只返回范围相关且状态为 `active` 的决策，并能说明选择与排除理由。
3. 决策被替代后，持有旧快照的 Agent 在任何写入前被确定性识别为过期。
4. 过期任务必须展示决策差异、刷新快照并重新规划；旧动作不得执行。
5. 对相同范围的互斥决策产生结构化冲突并进入 HITL。
6. 全部行为由内存存储、固定决策样本、mock LLM 和 mock 工具验证，不依赖网络、真实模型或真实 key。

#### 本轮设计变化

- 修改前：治理、反馈和记忆都可能作为主要贡献，项目主角不明确。
- 修改后：版本化决策记忆与上下文成为主贡献；治理和反馈围绕它提供执行保护和客观验证。
- 结果：三个候选方向均保留最低实现，但深度投入集中到决策版本、确定性选择、快照和过期重基线。

## T01 最终批准与结论摘要

- 批准时间：2026-07-16 14:36:16 +08:00
- 项目负责人确认（整理后的表述）：批准 T01 设计；本分支仅完成 T01，不处理 T02，并以独立的最后提交清空 `guiding.md`，确保提交历史清晰且便于后续合并到 `dev`。
- 产品定位：面向成员分别使用 Coding Agent、通过 Git / MR / CI 汇合的小型项目开发团队，构建“决策感知型 Coding Agent Harness”。
- 真实问题：跨成员、跨会话的项目决策断裂，使 Agent 在过期或缺失约束下行动，冲突延迟到 MR 阶段才暴露。
- 主要贡献：版本化决策记忆与上下文，包括不可变决策历史、范围感知选择、可复现快照、过期检测、重基线和结构化冲突。
- 基础闭环：自研主循环、受限工具、HITL、客观传感器、安全配置和脱敏 Trace 均提供可运行最低实现。
- 确定性要求：移除真实 LLM 后，六维机制仍可由脚本化 mock LLM、mock 工具和固定决策样本离线验证。
- 非目标：万能 Agent、模型训练、完整聊天存档、替代 Git / MR / CI、大型多租户强合规平台和无人监督执行。
- 过程证据：关键迭代 1 收敛用户与场景；关键迭代 2 将共享上下文转化为代码机制；关键迭代 3 比较三个方向并选择主要贡献。
- 范围边界：T01 只产出已确认设计输入与过程证据；本分支不创建 `SPEC.md`，不规划或执行 T02，不编写任何实现代码。
- 分支收尾：完成 T01 审计提交后，下一个且最后一个提交只清空 `guiding.md`，不夹带其他改动，便于后续合并到 `dev`。

## T02 规约形成记录

### 2026-07-16 · 从 T01 设计输入到正式产品需求

- 当前分支：`docs/t02-requirements-spec`；基线为合入 T01 的 `dev`。
- 使用 Skill：`using-superpowers`、`brainstorming`。
- AI 建议：把 T01 的角色化推演转换为可验收需求时，必须明确区分“产品假设”和“已验证事实”，不得把模拟频率写成实测成效。
- 项目负责人采纳：保留“小型项目团队、三个核心场景、决策连续性和版本化上下文”的定位，并确认首版采用人工登记与 Agent 提议，只有人可以激活或替代正式决策。
- 项目负责人修改：具体公网平台暂不决定，最迟在 T19 处理；T02 只固定单实例、持久化磁盘、HTTPS 和 Secret 注入等部署约束。
- 项目负责人拒绝：不自动读取聊天、MR 或会议内容生成正式决策，避免把模型推断当成团队约束。
- 规约变化：新增明确角色、九条 INVEST 用户故事、量化的可重复成功信号和“单实例、不自动抽取决策”等非目标。

### 2026-07-16 · 功能模块契约化

- AI 建议：九类功能不能只写愿望，应统一写输入、确定性行为、输出、边界、错误和可执行验收，并给关键验收分配稳定编号。
- 项目负责人采纳：批准人工登记、四级范围、不可变版本、写前过期阻断、Rebaseline、结构化冲突、受限工具、Trace 和加密凭据流程。
- 项目负责人修改：部署平台继续保留为后续决策，不影响本阶段的凭据和运行边界；服务重启采用明确中断而不是自动重放副作用。
- 项目负责人拒绝：不使用自然语言或 LLM 结果作为自动冲突判定，不用“更具体规则”静默覆盖全局冲突。
- 规约变化：形成九个模块的完整契约，并明确错误码、依赖和离线验收条件。

### 2026-07-16 · Harness 六维机制与主要贡献确认

- AI 建议：把产品功能重新映射到决策、工具、记忆、治理、反馈、配置六维，明确哪些必须自研、哪些只是底层库，并为每个维度写移除真实 LLM 后的直接断言。
- 项目负责人采纳：批准自研主循环、受限工具、确定性选择与快照、HITL、反馈回灌和脱敏 Trace；批准 `mock LLM + OpenAI-compatible` 的首版适配范围。
- 项目负责人修改：明确“前后端逻辑分离、生产单容器、后端模块化单体”，避免把部署单元与前后端职责混淆。
- 项目负责人拒绝：不使用现成 Agent Runner 充当内核，不用自然语言提示代替冲突、安全和完成条件。
- 主要贡献：不可变决策版本、范围感知选择、规范快照、写前阻断、Rebaseline 和结构化冲突；治理和反馈保留完整最低实现但不扩展为通用平台。

### 2026-07-16 · 架构与数据边界确认

- 比较方案：模块化单体、API/Worker 双进程、事件驱动多服务。
- AI 推荐：模块化单体最能把周期投入集中在版本化决策上下文，同时用接口、受限子进程和持久化事件维持隔离。
- 项目负责人采纳：选择模块化单体；React/Fastify 前后端逻辑分离，生产由单容器部署，后端内部按领域模块隔离。
- 项目负责人追问并确认：模块化单体与前后端思想不矛盾；开发可使用 Vite/Fastify 两进程，生产可由 Fastify 提供静态前端。
- 项目负责人拒绝：首版不引入独立 Worker、消息队列、微服务和 PostgreSQL，避免任务租约、重复消费和分布式运维稀释主要贡献。
- 数据决策：不可变版本、规范快照、追加 Trace、单次审批和服务端状态机形成一致模型；外部依赖失败时采用明确失败或保守停机。

### 2026-07-16 · 非功能与凭据安全决策

- AI 建议：把“性能好、安全可靠”替换为固定数据规模、`p95`、硬性布尔不变量和可复现攻击样本；把凭据安全写成资产—入口—预防—检测—恢复—验证闭环。
- 项目负责人采纳：SQLite 单实例、4 个并发任务、30 Step、3 次失败/Rebaseline、120 秒命令、64 KiB 输出、30 天 Trace 等默认边界。
- 项目负责人采纳：使用 AES-256-GCM 加密凭据；本地主密码隐藏输入，容器/线上使用 Secret，明文只在一次后端模型请求中短暂存在。
- 项目负责人修改：学校 Key 只用于手动真实适配器测试和受控 smoke test，核心测试、演示和 `unit-test` job 不得使用。
- 项目负责人拒绝：不把 `.env` 作为默认安全存储，不让子进程继承模型 Key，不在重启后自动重放副作用。

### 2026-07-16 · 技术、分发与部署决策

- AI 推荐：TypeScript、Node.js、Fastify、React/Vite、SQLite/Drizzle、Zod、Vitest、Playwright，使用 Docker 作为主要分发；项目负责人全部采纳。
- 数据库比较：JSON 文件缺少事务、索引和并发保护；PostgreSQL 对首版单实例增加运维。项目负责人确认 SQLite 单实例边界，并保留 Repository 迁移接口。
- LLM 决策：`ScriptedMockLLM + OpenAI-compatible`；学校 Key 只用于手动测试，供应商 Agent Runner 被拒绝。
- 前后端决策：逻辑分离，开发可分进程，生产单容器；共享 DTO/Schema，不共享数据库访问代码。
- Git 平台差异：课程通用章节提及 GitHub/Actions，但最终清单、当前远端和项目流程要求 GitLab、MR、`.gitlab-ci.yml` 与精确名称 `unit-test`；项目负责人采纳 GitLab 作为本项目规范。
- 部署修改：具体平台按负责人要求推迟到 T19；当前只批准平台能力门槛、单副本架构、mock 公网演示和真实调用受控要求。
- 拒绝项：首版不做微服务、独立 Worker、向量库、单文件二进制、多项目和匿名真实模型调用。

### 2026-07-16 · 验收追踪与风险收敛

- AI 建议：用唯一需求编号把用户故事、功能模块、通过/失败条件、验证层级、外部依赖和 T05–T20 关联起来，避免“功能写了但不知道怎么验收”。
- 项目负责人采纳：核心机制全部使用离线 mock；真实网络和 Key 只出现在受控线上 smoke test。
- AI 建议：把平台、Registry、依赖版本、Open Design 主题、生产 Secret 来源和学校 Provider 参数保留为有截止阶段的未决事项，而不是强行提前猜测。
- 项目负责人修改：部署平台暂不处理，因此 `OPEN-01` 保守默认明确为“不部署、不上传学校 Key”。
- 项目负责人拒绝：不以“后面再实现”作为无约束占位；每项都有负责人、候选、处理阶段和默认行为。
- 追踪结果：25 条需求覆盖 T01 六条成功判据、九个用户故事、六维机制、三项演示、非功能、CI、分发和最终审计。

## T02 最终批准摘要

- 批准时间：2026-07-16 16:53:08 +08:00。
- 批准范围：`SPEC.md` 第 1–12 节全部批准，G1 设计确认通过；本结论不授权实现，T03 和 G3 仍是必要前置。
- 相对 T01 的新增：九条 INVEST 用户故事、九个模块契约、25 条追踪需求、完整架构与数据状态、量化非功能指标、凭据威胁模型、GitLab CI、Docker 分发和部署能力门槛。
- 相对 T01 的细化：把“版本化决策记忆”落实为不可变版本、四级范围、规范快照、写前检查、Rebaseline、结构化冲突和人工激活；把六维 Harness 机制落实为可离线断言的接口和三项演示。
- 项目负责人采纳：TypeScript 全栈、模块化单体、前后端逻辑分离、SQLite 单实例、加密凭据库、ScriptedMockLLM、OpenAI-compatible 适配器、Docker 和 GitLab CI。
- 项目负责人修改：具体公网部署推迟至 T19；学校 Key 仅限人工集成与受控 smoke test；开发可前后端分进程，生产保持单容器。
- 项目负责人拒绝：首版不使用现成 Agent Runner、自然语言自动裁决、微服务、独立 Worker、PostgreSQL、向量库、匿名真实模型调用或重启后自动重放副作用。
- 最终自审修订：补明四种结构化约束的值类型；把 Action 生命周期拆成互斥合法路径；删除状态图中容易被误解为可写操作的自循环。
- 后续纪律：批准版本提交后，下一个提交只清空 `guiding.md`；合入 `dev` 后才开始 T03，且实现前必须通过 G3。

## T03 实现计划形成记录

### 2026-07-16 · 目录、接口与任务边界

- 当前分支：`docs/t03-implementation-plan`；基线为已合入 T02 的最新 `dev`。
- 使用 Skill：`writing-plans`；本阶段只修改文档，不创建实现文件。
- 候选一：按技术层建立少量大型文件。拒绝原因：领域状态、Runtime、路由和持久化会被混合，新鲜智能体难以独立评审。
- 候选二：立即拆成微服务和独立 Worker。拒绝原因：偏离已批准的模块化单体并引入分布式一致性，稀释主要贡献。
- 最终采纳：按单一职责拆分 `domain`、`application`、`runtime`、`infrastructure`、Fastify API、React WebUI 与 shared DTO；生产仍为单容器。
- AI 建议：冻结跨任务实体、Clock/ID/Hasher 注入、状态机与统一脱敏器的归属，防止 T05–T20 重复定义。
- 项目负责人已批准的上游约束：TypeScript 全栈、SQLite 单实例、ScriptedMockLLM、自研 Agent Loop、Docker 和 GitLab CI；T03 只细化接口，不改变这些决定。
- 修改：将 SPEC 的语义接口细化为稳定文件责任和 `LLMProvider`、`ToolExecutor`、`FeedbackSensor` 等端口，同时保留 Repository 可替换边界。
- 拒绝：不把领域规则写入 Fastify route、React component 或 Drizzle hook；不在 T03 提前选具体依赖版本。

### 2026-07-16 · T05–T07 工程基础与分发边界

- 采纳顺序：先建立无业务的工程/测试入口，再实现单次 LLM 抽象，最后实现严格 Action 与工具分发；该顺序让每一步可独立红绿验证。
- 接口修改：保留 SPEC 的正式名称 `LLMProvider`；`guiding.md` 中的“LLMClient”解释为同一单次调用边界，不再新增第二套同义接口。
- 采纳：`ScriptedMockLLM` 记录不可变调用、显式处理脚本耗尽和取消；供应商错误只分类，不在 Provider 内决定重试。
- 拒绝：T07 不顺手实现文件工具、Policy 或 Agent Loop；这些分别属于 T08、T09、T13。
- 冲突处理：T05–T07 共享根配置和包导出，故一级任务串行合并，不允许未合并 worktree 互相复制成果。

### 2026-07-16 · T08–T10 执行、治理与反馈

- 采纳：把“拒绝返回错误”加强为“拒绝且副作用计数为零/目标字节不变”，直接证明护栏位置正确。
- 采纳：T08 只执行已授权工具，T09 只在执行前裁决，T10 只在执行后产生客观事实，三层互不代替。
- 修改：guiding 的 `CODE_FAIL/POLICY_FAIL` 不作为新持久化枚举；分别映射到 SPEC 已批准的 `FAIL/CONFLICT`，避免类型分叉。
- 拒绝：不让 Policy 自动覆盖 deny，不让反馈引擎自动重试副作用，不用自然语言冲突判断。
- 跨平台：Windows junction 无创建权限时必须留下明确 skip 和人工补证，不能通过放宽 realpath 围栏解决。

### 2026-07-16 · T11–T13 主要贡献基础与核心闭环

- 采纳：版本/范围/规范 JSON/快照/Rebaseline 数据协议集中在 T11，Trace 与脱敏在 T12，主循环只在 T13 装配，避免循环模块吞并领域算法。
- 采纳：所有时间、ID、哈希和数据库顺序均可注入/规范化；并发激活与 Trace 序号以事务和唯一约束双重保护。
- 修改：T09 预先创建最小 `SnapshotBinding`，T11 必须保持其语义并提供正式 `ContextSnapshot`，禁止平行定义第二种绑定。
- 拒绝：不保存完整聊天或隐藏思维链；不在 Trace 失败时显示完整成功；不在重启后自动重放副作用。
- G4：只在 T13 合并且 mock 闭环、完成门、全部停机、stale/Rebaseline 与恢复语义通过后放行。

### 2026-07-16 · T14–T16 深度、演示与界面

- 采纳：T14 不增加功能面，而用变形、mutation、fault injection、上限和性能测量深化已批准主贡献。
- 采纳：T15 演示只装配生产接口，禁止演示专用绕过；每项用 mutation 证明断言能抓住机制缺失。
- 采纳：T16 的 WebUI 是状态与证据的投影，所有安全裁决、审批绑定和完成判断留在后端。
- 修改：T16 可与 T14 部分并行，但 shared DTO/状态机一旦变化即转为串行；T15 仍严格依赖 T14。
- 拒绝：不增加可视化工作流编辑器、聊天历史、移动端或前端安全规则副本。

### 2026-07-16 · T17–T20 安全与交付

- 采纳：凭据加密、真实 Provider 和全通道泄露测试集中在 T17，避免早期任务接触真实 Key。
- 采纳：CI 先以离线 contract/失败注入证明门禁有效；分发再以容器重启和新机器 smoke 证明不是开发机偶然成功。
- 采纳：T19 的平台/Registry/Secret 选择必须有实测和人工批准；能力不足时执行“不部署、不宣称”的保守默认。
- 修改：真实网络只允许 OPEN-06 批准后的单次 smoke；Git 历史扫描放在 T20，发现问题停止并人工处置，不自动破坏历史。
- 拒绝：不把 Key 写命令参数、`.env`、镜像、CI 或子进程；不以无持久磁盘/多副本 SQLite 平台伪装部署完成。

### 2026-07-16 · 依赖、并行与需求覆盖审查

- 采纳：主关键路径保持串行，只有纯测试/页面原型在接口冻结后有限并行，避免 worktree 共享文件冲突。
- 采纳：建立根配置、DTO、状态机、DB Schema、Trace、API、WebUI、Docker/CI 和 README 冲突矩阵。
- 采纳：每个新鲜 subagent 只接收当前任务最小 context；发现未定义合同必须暂停，不允许靠历史聊天猜测。
- 修改：T16 不在关键路径的 T14 前等待，可并行准备，但必须在 T17 前合并；这保留效率且不破坏凭据/UI 依赖。
- 覆盖：US-01–09、全部 NFR、DEMO-01–03、OPEN-01–06 和 REQ-001–025 均有唯一主要任务或明确验证位置。

### 2026-07-16 17:54:49 +08:00 · T03 技术自审（待负责人批准）

- AI 建议并采纳：所有 Task 的 `Files` 必须可机械定位；将“测试文件”“导出文件”“from map”等概括描述展开为 Create/Modify/Test 精确路径。
- AI 建议并采纳：补齐 `packages/infrastructure`、`packages/application` 的创建归属，把 `ports.ts` 放在最早消费 Clock/Hasher 的 T09，把凭据领域类型放在 T17，消除跨任务悬空依赖。
- AI 建议并采纳：为 T08–T20 加入可复制的局部 RED/GREEN 与收尾验证命令；T05–T07 已在步骤内给出同等命令。
- AI 建议并修改：原计划中部分 Step 包含一组紧密 RED/GREEN 动作；新增统一规则，若执行时不能在 2–5 分钟形成单一可验证结果，必须先在当前 Task 的 `guiding.md` 拆分，不得扩大行为。
- AI 拒绝：不把执行台账的“—/未开始”视为内容占位符；这些单元格有明确状态语义，只能在对应 Txx 产生真实 commit/MR/Pipeline 后填写。
- 覆盖审计：16 Task、16 Files、16 Interfaces、16 完成标准、16 独立分支；`REQ=25`、`US=9`、`DEMO=3`、`OPEN=6`。
- 范围审计：相对 `dev` 只有四个 T03 文档；未创建实现代码、工程配置、测试、Dockerfile 或 CI。
- 当前结论：技术自审通过，版本为 `1.0.0-rc1`；项目负责人尚未对生成后的 PLAN 逐章确认，因此 G2 仍未通过，不提前伪造人工批准。

### 2026-07-16 18:02:22 +08:00 · T03 最终批准

- 项目负责人结论：批准当前 `PLAN.md` 的 T05–T20、依赖/并行边界、文件冲突矩阵、验证合同和 T04 冷启动说明，无追加修改。
- 最终批准版本：`PLAN.md` 1.0.0；G2 计划确认通过。
- 采纳：保留技术自审对精确路径、验证命令、package/port/type 归属和 2–5 分钟拆分规则的全部修订。
- 拒绝：没有新的拒绝项；执行阶段仍不得把待定平台、版本或真实 Provider 参数猜测成已批准事实。
- 修改：仅将 `1.0.0-rc1` 晋升为 `1.0.0` 并写入批准时间与 G2 结论，不改变 SPEC 1.0.0 产品边界。
- 后续纪律：下一提交只清空 `guiding.md`；T03 合入 `dev` 后进行 T04，G3 通过前不得创建实现代码或工程配置。

### 2026-07-16 23:52:23 +08:00 · T04 主控预审修订

- 来源：以下问题由主控在 T04 冷启动前复核 T03 产物时发现，不是陌生智能体发现，也不会计入冷启动发现数量。
- 原问题一：T13 的 G4 命令引用未在该 Task `Files` 中声明的 `tests/integration/runtime/core-loop.test.ts`，执行者无法从计划确认其创建责任。
- 修订一：G4 改为运行 T13 已声明的 `tests/integration/agent/loop.test.ts`、`stale-rebaseline.test.ts` 和 `restart-recovery.test.ts`。
- 原问题二：T15 的验证命令保留 `demos/mechanisms/<name>.test.ts` 占位路径，无法直接复制执行。
- 修订二：将占位路径展开为 deny-dangerous-action、feedback-recovery、stale-rebaseline 三个明确测试文件。
- 原问题三：部分 Step 把测试、实现、评审、修复、提交、台账、清空 guiding、等待 Pipeline 或合并 MR 等多个可独立验证的动作捆绑在一起。
- 修订三：统一显式标注每个 Step 为 2–5 分钟，并重点拆分 T05、T06、T07、T09、T10、T11、T13、T14、T16、T17、T18、T19、T20 的复合动作；评审、Critical 修复、回归、提交与收尾分别留下独立结果。
- 边界判断：本次只修复 `PLAN.md` 内部路径和执行粒度，不修改 `SPEC.md`，不改变产品范围、接口语义、Txx 分工、依赖 DAG 或主要贡献。
- 版本判断：`PLAN.md` 从 1.0.0 修订为 1.0.1；项目负责人已明确要求在 T04 第二个提交先修复 T03 问题，因此 G2 保持通过，G3 仍未通过。
- 冷启动纪律：后续陌生智能体只能接收修订后冻结的 `SPEC.md` 与 `PLAN.md`；不得把本节已知问题包装成其独立发现。

### 2026-07-17 · T04 冷启动协议与输入冻结

- 协议范围：建立可复现的陌生智能体冷启动步骤、原始 prompt、隔离规则、原始问题模板、五类结果模板和预期差距模板；本阶段没有执行 Gemini 试做，也没有填写不存在的结果。
- 智能体边界：主开发智能体为 OpenAI Codex App；目标陌生智能体为 Google Gemini 网页版，供应商和产品类型均不同。用户当前称界面为“Gemini 3.5 Flash”，但执行当天必须以网页实际显示的完整模型名称、时间和截图为准，当前不将该标签记为已验证事实。
- 全新会话证据：执行时需记录新建空会话、无历史、memory/个性化状态、实际完整模型名称、验证时间和截图；若 memory 无法关闭或判断，按环境缺口暂停，不得补充历史或口头解释。
- 冻结输入：HEAD/输入提交 `b337e5ef3decb6f71a5216922f94feee05c3b2d0`。`SPEC.md` blob 为 `5bc380d4bf5854a07d0f65e06c517ffaa81cfd66`、SHA-256 为 `a22e11b49d489ac81d7dbfa08d8c3746a89669d6fedfaba0d42a2e88f19c2f6f`、1305 行；`PLAN.md` blob 为 `8690e3bd56894fffd7b64d4c5b2ed6c253ac2b38`、SHA-256 为 `1f88a5f9b36634200ece0198cf9dcae611cc0cd1fef918475b700c498c378a94`、1174 行。
- 唯一输入：只上传上述冻结的 `SPEC.md` 与 `PLAN.md`；不提供仓库、项目历史、日志、`guiding.md`、课程 TODO、主控预审答案、口头补充或 API key。
- 环境判断：Gemini 网页可能没有本地终端、Git 仓库或文件写入能力。执行时如实记录实际工具；工具缺失归类为环境缺口，禁止伪造命令、测试、diff 或文件修改，但仍须选择 1–2 个 PLAN Task 在能力范围内试做并在不确定时暂停提问。
- 修订判断：当前只冻结验证方法与输入证据，未产生可用于修订 `SPEC.md` 或 `PLAN.md` 的冷启动发现；G3 仍未通过。

### 2026-07-17 · T04 冷启动发现分类与修订决策

- 输入证据：提交 4 `325ab7f` 保存的首次 Gemini 回复、两张截图，以及用户在同一冷启动会话/同一冻结输入上“重新生成一次”得到的补充文本。补充文本没有新截图、独立时间或隔离证据，不替代首次结果，也不作为修订后复验。
- **采纳进入修订队列：** `PLAN.md:14,36–37,1163` 同时要求 G3 前禁止实现、T04 试做 1–2 个 Task，却没有定义隔离的一次性文本草案/模拟执行与正式仓库实现的边界。首次回复在 T05 Step 1 完全停住，补充回复则认为非业务 `health.ts` 可草拟；相反结果证明这是阻断新鲜智能体的一项真实 PLAN 缺陷。拟在提交 6 只澄清 PLAN：允许范围、隔离位置、禁止合入和不能宣称 T05 开始；不改变产品需求、T05 接口或 G3 正式实现禁令。
- **拒绝修改 OPEN-03：** `SPEC.md:1285` 和 `PLAN.md:232` 已明确 T05 实现者提出版本、项目负责人批准，未决时保守不安装依赖/不创建骨架。Gemini 问“谁批准”是误读，暂停等待版本裁决则符合有意的人类决策门；T04 不提前猜 Node/Fastify/TypeScript 版本。
- **拒绝向 T05 追加包：** 补充回复建议在 T05 新建 runtime/infrastructure package；`PLAN.md:299–302` 与 `:426` 已分别把创建责任交给 T06/T08。提前创建会违反 Task 文件边界，不因智能体误读修改正确计划。
- **拒绝把非业务源码视为 G3 例外：** `PLAN.md:14` 禁止源码、测试和依赖，不限业务机制；T05 的“不实现任何业务机制”不能授权 G3 前编写 `health.ts`。该误读只用于验证边界缺陷，不改变全局禁令。
- 环境处置：无终端/Git/写入属于能力缺口；`personal_context` 可用且 memory/个性化关闭未证明，使本轮不能被认定为完全合格冷启动；页面只显示“Flash”，完整模型名也未核验。提交 7 复验必须使用全新且 memory 状态可证的会话并补齐模型证据，或者如实保留不可核验结论。
- SPEC 判断：现有证据没有支持 `SPEC.md` 需求或架构缺陷，也没有证明 SPEC/PLAN 方向不一致；`SPEC.md:1303` 与正式实现禁令一致。因此提交 6 不应为凑缺陷改动 SPEC，除非负责人另行确认真实需求变化。
- Gate：F-01 完成负责人批准、PLAN 修订和同 T05 全新会话复验前，G3 不得通过。
