# AI4SE Coding Agent Harness 规约形成过程

## 文档用途

本文件记录 T01 brainstorming 中的问题、角色化访谈推演、AI 建议、项目负责人判断和设计变化，为 T02 编写正式 `SPEC.md` 提供可追溯的设计输入。模拟目标用户的回答必须明确标注，不作为真实外部用户调研数据；未经项目负责人确认的内容不得写成最终结论。项目负责人的口语化表达可以在不改变原意的前提下整理，并标注为“整理后的表述”。

## 记录模板

### 关键迭代 N：主题

- 问题：
- 参与者回答（真实或模拟必须标注）：
- AI 建议：
- 本人判断：
- 设计变化：

## 初始设想

- 项目方向已确定为 A：Coding Agent Harness。
- 项目应自研 Harness 内核，而不是用现成 Agent Runner、提示词、Skill 或配置文件冒充核心机制。
- 六个维度都需要可运行的最低代码实现：决策、工具、记忆、治理、反馈、配置。
- 核心机制必须能在移除真实 LLM 后，由 mock / stub LLM 驱动确定性测试。
- 当前尚未确认具体产品、目标用户、结构性问题、能力边界和主要贡献维度。

## 已知硬约束

- T01 只允许需求探索与文档记录，不创建 `SPEC.md`，不编写实现代码。
- brainstorming 必须逐问逐答，至少比较三个真实可行方案。
- 必须形成陌生人可在 30 秒内理解的价值陈述，并明确为什么不直接使用现成 Coding Agent。
- 必须明确非目标，排除万能 Agent 和无边界自主执行。
- 必须为 Harness 六个维度形成最低实现边界与确定性验证思路。
- 必须选择一个机制密集的主要贡献维度，并记录采纳、拒绝或修改理由。

## 待回答问题

以下仅为待讨论主题，不代表已经确认的结论：

1. 目标用户是谁，其当前工作方式是什么？
2. 用户最频繁、代价最高且适合由 Coding Agent Harness 改善的问题是什么？
3. 至少三个具体使用场景的触发、输入、期望结果和失败影响是什么？
4. 为什么现成 Coding Agent 不能充分解决该问题？
5. 项目的明确非目标是什么？
6. Agent 可以做什么、禁止做什么？
7. 工具、反馈、治理、记忆、主循环、配置与可观测性的最低机制是什么？
8. 治理、反馈闭环、运行控制、记忆与上下文等候选主要贡献方向中，应选择哪一个？

## Brainstorming 记录

### 关键迭代 1：模拟目标用户访谈——从泛化 Harness 到团队决策连续性

- 访谈方式：AI 扮演小型项目开发团队成员，围绕目标用户、工作方式、痛点、代价和使用场景连续回答问题；项目负责人在访谈结束后审阅并确认设计结论。
- 真实性说明：下列“模拟用户回答”是角色化需求推演，不代表已完成真实外部用户访谈。

- 问题：这个 Coding Agent Harness 的首要目标用户是谁？
- 模拟用户回答：小型项目开发团队。
- AI 建议：先把“小型项目开发团队”作为唯一的主要用户群体，再继续确认团队规模、当前 Coding Agent 协作方式和最突出的问题，避免同时面向个人开发者与高合规大型团队而扩大范围。
- 追问：团队当前最接近哪种工作方式？
- 模拟用户回答：每个成员各自使用 Codex、Claude Code 等 Coding Agent，最后通过 Git / MR / CI 汇合。
- 追问：这种协作方式目前最严重的问题是什么？
- 模拟用户回答：需求、历史决策和上下文分散在不同成员的会话中，后续 Agent 容易误解或重复劳动。
- 追问：上下文与历史决策分散后，团队付出的最大代价是什么？
- 模拟用户回答：新改动违背旧设计约束，合并时才发现冲突。
- 追问：这个问题是实际高频问题还是项目假设？
- 模拟用户回答：在该用户画像下，预计每个迭代会遇到一至两次；该数字是假设，后续需要通过真实试用校准。
- 本人判断：形成“上下文分散 → Agent 不知道既有设计约束 → 新改动偏离约束 → MR 合并阶段才暴露”的首要问题链；发生频率暂按产品假设处理，不把模拟回答写成实测数据。
- 设计变化：产品价值不再泛指“共享上下文”，而是要让后续 Agent 在行动前获得可追溯的有效设计约束，尽早阻止违背旧决策的改动。

#### 使用场景 1：依赖或配置变更

- 触发：团队成员让 Agent 升级依赖或调整项目配置。
- 输入：本次依赖或配置变更目标、相关项目文件，以及团队既有的配置文件格式和环境变量接口约束。
- 期望结果：Agent 在改动前获得相关约束；若拟议改动会改变配置文件格式或环境变量接口，Harness 必须暂停执行，展示发生冲突的历史约束及其来源，并请求人工批准。
- 失败影响：Agent 擅自改变已有配置文件格式或环境变量接口，直到 MR 合并阶段才暴露并引发返工，也可能破坏现有部署或调用方。
- 状态：模拟访谈已形成完整场景，项目负责人已审阅确认。

#### 使用场景 2：并行开发中的决策版本漂移

- 触发：不同成员并行开发，分别让自己的 Agent 执行不同任务。
- 输入：两个并行任务、各自工作分支，以及各 Agent 获得的项目决策版本。
- 期望结果：Harness 识别 Agent 使用的项目决策版本是否过期；发现过期时暂停任务，展示决策差异，刷新上下文，并要求 Agent 根据最新决策重新规划后再继续。
- 失败影响：两个 Agent 基于不同版本的项目决策产生互相冲突的改动，直到 MR 汇合时才发现，导致返工或错误合并。
- 状态：模拟访谈已形成完整场景，项目负责人已审阅确认。

#### 使用场景 3：新成员或新 Agent 接手任务

- 触发：新成员加入项目，或团队为新任务启动一个没有历史会话的新 Agent。
- 输入：任务目标、当前代码库状态、带版本和来源的团队决策记录。
- 期望结果：Harness 只选择与当前任务相关且仍然有效的决策，生成精简上下文包；每条约束保留来源、版本和适用范围。
- 失败影响：新成员或新 Agent 需要翻阅全部聊天记录，仍可能遗漏隐含约束、重复解释背景或按错误假设实现功能。
- 状态：模拟访谈已形成完整场景，项目负责人已审阅确认。

### 项目负责人审阅结论：产品价值与边界

#### 产品定位

为成员分别使用 Coding Agent、最终通过 Git / MR / CI 汇合的小型项目开发团队，提供一个“决策感知型 Coding Agent Harness”。现有 Coding Agent 继续负责代码生成；本 Harness 将散落在不同会话中的有效设计决策转化为带版本、来源和适用范围的约束，并在 Agent 行动前按任务检索、检查和治理。

#### 30 秒价值陈述

面向成员分别使用 Coding Agent 的小型开发团队，本项目提供一个可追溯、版本化的团队决策 Harness。它在 Agent 编码前提供当前任务真正需要的设计约束，并在发现过期决策或契约冲突时暂停执行、请求人工确认，从而把原本在 MR 阶段才暴露的设计冲突提前到行动之前。

#### 为什么不直接使用现成 Coding Agent

- 现成 Coding Agent 负责生成代码，本项目不重复实现其代码能力。
- 结构性缺口是团队自己的跨成员、跨会话决策连续性。
- 普通规则文件较静态，难以表达决策版本、来源、适用范围和失效状态。
- 本项目用自研代码提供确定性的版本检查、契约冲突检测、HITL 和审计链。

#### 非目标

- 不做万能 Coding Agent，不训练自己的模型。
- 不替代 Git、MR、CI 或现有 Coding Agent。
- 不允许无边界、无人监督的自主执行。
- 不保存全部聊天记录，只保存对未来任务有结构性价值的决策。
- 首版不处理大型企业、多租户和强合规场景。
- 不把 prompt、Skill、规则文件或现成 Agent Runner 算作 Harness 内核。

#### 负责人确认

- 批准时间：2026-07-16 12:00:56 +08:00
- 负责人确认（整理后的表述）：认可上述产品价值、使用场景和范围边界，并继续完善 Harness 六维机制设计。
- 结论：项目负责人批准角色化访谈产出的产品价值、三个使用场景、现成 Agent 的结构性缺口和非目标，将其转为 T02 可使用的确认设计输入。

### 关键迭代 2：模拟机制访谈——从“共享上下文”到可执行的决策治理

- 访谈方式：AI 继续扮演目标团队成员，回答 Agent 能力边界、六维最低机制、安全策略和验证方式；下列回答属于角色化需求推演。
- 问题：Agent 最低限度应该完成哪些任务？
- 模拟用户回答：读取任务相关代码和有效决策，在工作区内修改文件，运行测试、lint、类型检查和构建，提出新的决策建议，并在验证通过后给出结果摘要。
- 问题：哪些事情明确禁止自动完成？
- 模拟用户回答：不得修改或删除已生效决策，不得绕过审批，不得读取凭据，不得访问工作区外路径，不得危险删除、联网部署或强推，不得把未验证结果声明为完成。
- AI 建议：把“决策连续性”落实为结构化 `DecisionRecord`、任务级 `ContextSnapshot` 和确定性的版本、契约检查；LLM 只提出动作，不负责判定动作是否安全或合规。
- 本人判断：接受该边界。普通聊天摘要、prompt 或规则文件只能提供信息，不能代替自研主循环、工具分发、冲突检测、审批状态机和反馈回灌。

#### 决策主循环

1. 接收任务目标、代码库状态和预算。
2. 按文件路径、模块和标签检索有效 `DecisionRecord`，生成带摘要值的 `ContextSnapshot`。
3. 通过可注入的 LLM 抽象层进行一次决策，输出结构化 Action。
4. 解析并校验 Action；未知动作或参数错误转换为结构化 Observation。
5. 检查上下文版本、路径边界、契约冲突和权限策略。
6. `ask` 动作暂停并等待 HITL；审批前工具不得执行。
7. 分发工具，记录 ToolResult，再运行反馈传感器。
8. 把工具结果、冲突或验证结果回灌下一轮上下文。
9. 仅在验证通过且不存在未处理冲突时完成；最大步数、预算耗尽、连续失败、审批拒绝、人工取消或环境故障均可停机。

#### 动作与工具

| 工具 | 输入 | 输出 | 边界与失败原则 |
| --- | --- | --- | --- |
| `decision.query` | 任务、路径、模块、标签 | 有效决策及 ID、版本、来源 | 只读；不得返回已失效决策作为当前约束 |
| `decision.propose` | 候选决策、理由、适用范围 | `proposed` 状态记录 | 不能自动转为 `active` |
| `file.read` | 工作区内相对路径 | 文件内容或结构化错误 | 解析真实路径并拒绝路径逃逸；敏感文件拒绝读取 |
| `file.write` | 相对路径、预期旧摘要、新内容 | 写入结果与新摘要 | 仅限工作区；写前检查快照和决策冲突 |
| `command.run` | 白名单命令、参数、超时 | 退出码、截断且脱敏的输出 | 不经 shell 拼接；限制目录、时间和输出长度 |
| `sensor.run` | 传感器名称、目标 | 结构化反馈 | 环境错误不得伪装为业务失败或通过 |

统一 `ToolResult` 至少包含 `status`、`data`、`error_code`、`redacted_output` 和 `evidence`。工具异常必须返回给主循环，不得吞掉错误或显示假成功。

#### 客观反馈

- `DecisionVersionSensor`：比较任务快照与当前有效决策版本，发现过期即返回 `CONFLICT`。
- `ContractDiffSensor`：比较配置格式和环境变量接口快照，检测删除、改名或不兼容类型变化。
- `CommandSensor`：运行测试、lint、类型检查和构建，保存退出码与摘要证据。
- 统一分类：`PASS`、`FAIL`、`CONFLICT`、`ENV_ERROR`、`TIMEOUT`。
- `FAIL` 或 `CONFLICT` 会进入下一轮上下文并要求重新规划；连续失败达到阈值后升级给人。

#### 危险动作与 HITL

- `allow`：读取普通项目文件、查询决策、运行 `git status` / `git diff` 和白名单验证命令。
- `ask`：改变配置格式或环境变量接口、激活或替代决策、修改共享核心文件、执行白名单外但可解释的非破坏性命令。
- `deny`：读取密钥或 `.env`、访问工作区外路径、危险删除、联网部署、强推、绕过验证或审批。
- 审批绑定动作类型、规范化参数、目标文件摘要、`ContextSnapshot` 摘要和有效期；任何内容变化都会使批准失效，批准只能消费一次。

#### 记忆与上下文

- `DecisionRecord` 字段：ID、标题、版本、状态（`proposed` / `active` / `superseded`）、适用路径或模块、结构化约束、设计理由、来源 commit / MR / 人工确认、创建时间和替代关系。
- `ContextSnapshot` 字段：任务 ID、代码状态摘要、选中决策的 ID / 版本集合、选择理由和整体摘要。
- MVP 使用路径、模块和标签做确定性选择，不引入向量数据库；新鲜 Agent 可以仅凭任务快照重建所需上下文。
- 任务结束清除临时消息和工具原始输出；长期保留有效决策和审计事件。
- API Key、token、密码、`.env` 内容、完整命令原始输出永不进入记忆。

#### 配置与可观测性

- 配置包含工作区、工具白名单、策略规则、传感器、最大步数、连续失败阈值、超时和输出上限。
- 配置必须通过 schema 校验；错误配置快速失败，默认策略采用最小权限。
- Trace 逐轮记录快照摘要、使用的决策 ID / 版本、LLM 动作、策略判定、审批状态、工具结果、传感器结果和停机原因。
- 日志自动脱敏并限制输出长度；Trace 可供单元测试、WebUI 和审计读取。

#### 六维最低代码机制与 mock 验证

| 维度 | 最低代码机制 | 不依赖真实 LLM 的验证 |
| --- | --- | --- |
| 决策 | 自研循环、Action 解析、预算与停机 | 脚本化 mock 依次返回动作，断言轮次和停机原因 |
| 工具 | 注册表、参数校验、受限文件与命令分发 | mock 工具记录调用，断言未知工具、越界路径和异常返回 |
| 记忆 | 决策记录、确定性选择、快照与清除 | 构造相关、无关和失效决策，断言只选择有效相关项 |
| 治理 | `allow` / `ask` / `deny`、审批状态机和动作摘要绑定 | 危险动作进入审批且工具调用为零；篡改参数后批准失效 |
| 反馈 | 版本、契约和命令传感器，结果回灌 | 第一次失败后 mock 改变动作，第二次验证通过 |
| 配置 | schema、安全默认值、脱敏 Trace | 错误配置快速失败，日志中不出现构造的假密钥 |

#### 本轮设计修订

- 修改前：用共享文档或聊天摘要保存“团队上下文”，由 LLM 自行理解是否冲突。
- 暴露的问题：内容缺少版本、来源和适用范围；冲突判断依赖 LLM，无法稳定复现和单测。
- 修改后：使用结构化决策记录、版本化任务快照和确定性传感器；LLM 负责提议，代码负责版本检查、契约检查、权限和停机。
- 结论：六个维度均有可运行的最低代码机制，且可以用 mock LLM 离线验证；本轮仍未选择主要贡献维度。
