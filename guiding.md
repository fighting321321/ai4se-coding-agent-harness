# T14：长期记忆生命周期

## 目标

把已有 `JsonMemory` 从孤立存储类接入 Agent 和 CLI：会话开始检索相关记忆，会话过程中收集安全候选，会话结束时确定性固化，重新启动后能够检索，并提供面向用户的 `/memory` 管理命令。完成后停在本分支，等待项目负责人手动合并回 `dev`。

## 固定边界

- 分支：`feat/t14-memory-lifecycle`。
- 以 `docs/assessments/FULL_HARNESS_REASSESSMENT.md` 第 3、6、7 节为准。
- 复用现有 `JsonMemory`、`Redactor`、T13 `SessionContext` 和 CLI，不另建数据库。
- 只实现长期 Memory 的检索、候选收集、SessionEnd consolidate、重启恢复和 `/memory`。
- 不实现通用 Hooks、Skill、MCP、子 Agent、传感器或 Checkpoint；这些属于 T15–T16。
- consolidate 必须可确定性离线测试；不得依赖真实 LLM 判断机制是否正确。
- 不得固化 API Key、密码、token、完整对话、大段文件正文、命令输出或敏感 Action 内容。
- 当前目录继续作为 Memory 的工作区边界，不跨项目共享。
- 本分支总提交数不得超过 7 个；首提交仅写本文件。

## 提交步骤

### 提交 1：任务指导

- 写入本 `guiding.md`。
- 不修改产品代码。

### 提交 2：建立 Memory 生命周期与安全候选模型

- 为会话级 Memory 定义清晰的候选、固化和检索边界。
- 从已完成任务中生成长度受限、可检索的 `recent_result`；只保留完成摘要和必要标签，不复制完整消息或 Observation。
- 对用户明确表达的稳定约定提供受限 `convention` 候选机制，不能凭空推断秘密或个人信息。
- 所有候选统一经过脱敏、长度限制、去重和稳定排序。
- 先添加失败测试，再完成最小实现。

### 提交 3：接入主循环与会话收尾

- 每项任务开始时按当前目标检索相关 Memory，并注入 LLM 上下文。
- 任务结束后把合格结果加入当前会话的待固化集合，不在每一步反复写盘。
- 在 `/new`、`/exit`、输入结束和可控异常收尾时执行 consolidate。
- consolidate 使用原子写入；失败必须返回可诊断但已脱敏的结果，不能破坏已有 Memory。
- 不引入 T15 的通用 Hook 框架。

### 提交 4：实现重启恢复与 `/memory`

- 新进程启动后能够从工作区 `.ai4se/memory.json` 检索之前固化的相关结果。
- `/memory` 显示安全、简短的记忆列表或空状态，不显示底层 JSON。
- 至少支持明确的清理操作，例如 `/memory clear`；清理前需要用户确认，测试可注入确认边界。
- `/new` 只重置短期对话并先固化当前候选，不得清空长期 Memory。
- Memory 损坏、不可读或写入失败时给出用户可理解的提示，不输出内部秘密或原始异常。

### 提交 5：集成验收与收尾

- 离线验证同一工作区重启后可检索上一会话固化的脱敏结果。
- 验证无关任务不会注入无关记忆，重复 consolidate 不产生无限重复项。
- 验证 Key、凭据、大段正文和工具输出无法进入 Memory、Trace 或普通输出。
- 验证 `/memory`、`/memory clear`、`/new`、`/exit` 与异常收尾的组合行为。
- 使用统一入口运行 `test`、`lint`、`typecheck`、`build` 和 `audit`；若遇到 `spawn EPERM`，获准后原样重跑。
- 更新必要的 README、SPEC、PLAN 和 AGENT_LOG 状态。
- 清空 `guiding.md` 后提交，确保本分支总提交数不超过 7 个。

## 完成标准

1. Memory 在主循环任务开始前被真实检索并注入，而不是只存在一个未使用的类。
2. 会话完成、`/new`、`/exit` 和输入结束时会固化安全候选。
3. 重新启动 CLI 后，相关新任务能检索先前会话的长期记忆。
4. `/memory` 能查看安全摘要并管理清理操作。
5. Memory 内容经过脱敏、限长、去重，不包含凭据、完整对话或大段工具输出。
6. 所有机制在 mock/stub 下有确定性离线测试，既有 T13 会话能力不退化。
7. 统一门禁通过，工作树干净，分支停在等待负责人合并的状态。

## 停止条件

- 不合并到 `dev`，不创建或合并 MR，不发布版本。
- 不读取 `.ai4se/temp-api-key.txt`；真实 Provider 验收由总控在实现完成后另行执行。
- 如果必须扩大到 T15–T16 范围，停止并向项目负责人说明。
- 如果预计超过 7 个提交，先停止并重新拆分，不得强行压缩历史。
