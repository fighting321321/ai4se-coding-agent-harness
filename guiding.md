# T16：受限子 Agent、反馈传感器与 Checkpoint

## 目标

为教学级 Harness 补齐复杂任务隔离、代码变更后的自动验证反馈，以及失败后的可控恢复：父 Agent 可串行委派受限子 Agent；写入后自动运行配置的传感器；副作用前建立明确范围的 Checkpoint，并在工具或传感器失败时恢复。完成后停在本分支，等待项目负责人检查并合并回 `dev`。

## 固定边界

- 分支：`feat/t16-subagent-feedback-checkpoint`。
- 以 `docs/assessments/FULL_HARNESS_REASSESSMENT.md` 第 2、3、6、7 节为准。
- 复用 T13–T15 的 SessionContext、Memory、Hooks、Skill、MCP、Policy、Approval、Dispatcher 和 Trace。
- 子 Agent 只允许串行执行，使用独立短期上下文、受限工具集合、最大深度和共享总步骤预算；只把脱敏摘要返回父会话。
- 不实现并行 Agent、Git worktree 隔离、网络沙箱、全屏 TUI 或生产级调度器。
- 自动传感器只接受结构化 executable/args 配置，默认覆盖 test、lint、typecheck 类验证；不得拼接 Shell 字符串。
- Checkpoint 采用教学级受控路径快照：只恢复明确纳入快照的工作区文件。不得声称能回滚未声明写集的任意外部命令、MCP 或系统副作用。
- 文件恢复必须逐个处理已确认的工作区内明确路径；禁止递归删除目录或批量删除未知文件。
- 新文件回滚只允许删除本次工具创建的单个已确认文件；目录和多文件清理由用户确认，不能静默扩大。
- 不读取 `.ai4se/temp-api-key.txt`，不使用真实 Provider，不记录或提交任何凭据。
- 不做 T17 的版本号、tarball、Release 或最终提交材料更新。
- 本分支总提交数不得超过 7 个；首提交仅写本文件。

## 提交步骤

### 提交 1：任务指导

- 写入本 `guiding.md`。
- 不修改产品代码。

### 提交 2：建立受控 Checkpoint 边界

- 定义可注入 Checkpoint 接口、快照元数据、恢复结果和固定错误码。
- 对 `write_file` 等明确目标副作用，在执行前保存文件存在状态、受限内容和必要元数据。
- 工具或后续传感器失败时恢复原文件；若文件原本不存在，只删除本次创建的那个明确文件。
- 路径必须经过工作区围栏和 realpath/父目录校验；拒绝 `.git`、凭据路径、目录、符号链接和超限文件。
- 快照不得包含 API Key、token、密码或未受限的大文件正文。
- 先添加失败测试，再完成最小实现。

### 提交 3：实现自动反馈传感器

- 定义结构化 Sensor 配置、执行器、结果分类、超时和输出上限。
- 代码写入成功后按稳定顺序运行启用的 test、lint、typecheck 传感器。
- 传感器结果经过脱敏后写入 Observation 和 Trace，并反馈给 Agent 决定下一步。
- 传感器失败触发对应 Checkpoint 恢复；恢复结果也必须进入 Trace。
- 传感器不得在 `read_file`、`load_skill`、纯查询 MCP 或 `finish` 后无条件运行。
- mock 测试覆盖成功、业务失败、超时、输出截断、秘密脱敏和恢复失败。

### 提交 4：实现串行受限子 Agent

- 增加明确的委派 Action 和子 Harness 边界，不允许子 Agent 直接复用父 SessionContext。
- 子 Agent 拥有独立短期上下文，只获得任务所需规则、Skill 名片和允许的工具。
- 默认子 Agent 采用只读或显式授权的最小工具集合；不得绕过父级 Policy、Approval、Hook 和工作区围栏。
- 限制最大委派深度、每个子 Agent 步骤数和父子共享总预算；超限时确定性停止。
- 子 Agent 结束后只向父会话返回长度受限、脱敏摘要，不回灌完整内部对话或大段输出。
- 使用嵌套 `ScriptedMockLLM` 离线验证成功、失败、深度上限和预算耗尽。

### 提交 5：整合治理、恢复和父子 Trace

- 将委派、传感器与 Checkpoint 纳入 Policy、Approval、Hooks 和统一反馈分类。
- Trace 记录父子会话关系、预算消耗、传感器结果、Checkpoint 创建/恢复及停止原因。
- 验证 PreToolUse 阻断时不创建无意义副作用，工具执行失败或传感器失败时恢复顺序稳定。
- 验证子 Agent 无法越权调用父级未授权工具，也不能把秘密带回父上下文或 Memory。
- 验证 T13–T15 的会话、长期记忆、Skill、MCP 和 Hook 行为不退化。

### 提交 6：集成验收与收尾

- 完成“父 Agent 委派只读分析”“写文件后传感器通过”“传感器失败并恢复”三类端到端离线场景。
- 验证深度/总预算守卫、Checkpoint 路径边界、恢复失败和异常收尾组合行为。
- 使用统一入口运行 `test`、`lint`、`typecheck`、`build` 和 `audit`；若遇到 `spawn EPERM`，获准后原样重跑。
- 更新必要的 README、SPEC、PLAN、AGENT_LOG 和重新评估矩阵。
- 清空 `guiding.md` 后提交，确保本分支总提交数不超过 7 个。

## 完成标准

1. 子 Agent 串行运行、上下文隔离，并受深度、步骤和共享总预算限制。
2. 子 Agent 只能使用允许的工具，只向父会话返回脱敏摘要。
3. 代码写入后自动运行配置传感器，并把结果回灌给 Agent。
4. 明确范围内的副作用在工具或传感器失败后可以确定性恢复。
5. 对无法安全快照的副作用明确拒绝或说明限制，不虚假声称已回滚。
6. 父子关系、传感器和 Checkpoint 事件进入脱敏 Trace，既有机制不退化。
7. 所有机制均有 mock/stub 离线测试，统一门禁通过，工作树干净。

## 停止条件

- 不合并到 `dev`，不创建或合并 MR，不发布版本。
- 不为追求完整回滚而递归复制或删除整个工作区，不使用危险 Git 重置命令。
- 如果实现需要任意命令副作用的生产级沙箱、Git worktree 或操作系统快照，停止并报告，不扩大课程项目范围。
- 如果预计超过 7 个提交，先停止并重新拆分，不得强行压缩历史。
