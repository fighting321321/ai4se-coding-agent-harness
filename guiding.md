
# T17：最终验收、分发包与 v2.0.0 Release 准备

## 目标

在不继续扩张课程项目范围的前提下，将 T13–T16 已完成的完整 Harness 能力统一收尾为可复现、可安装、可供助教直接测试的 v2.0.0 CLI 交付物。本分支只完成代码、测试、文档和 Release 材料准备；合并到 `dev`、真实 Provider 最终验收、创建标签及公开 Release 由总控后续完成。

## 工作原则

- 本任务所在分支为 `release/t17-harness-v2`，不得与其他 Txx 共用分支。
- 本分支提交总数不得超过 7 个（包含本指导提交），建议严格按下列 6 步提交。
- 保持课程作业所需的简易实现，不新增 WebUI、在线服务、数据库或生产级基础设施。
- 所有项目命令统一通过 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\project-env.ps1 <task>` 运行。
- 不读取或使用真实 API Key；不得把 Key、密码、Token 写入源码、测试、日志、Trace、文档、命令参数或 Git。
- 不创建虚假的公开地址。需要 GitHub/GitLab 网页操作时，只准备材料并记录人工步骤。
- 不合并 `dev`，不打标签，不创建或发布 Release，不推送 `main`。

## 提交步骤

### 1. 编写 T17 指导文件

- 仅提交本文件，明确范围、提交顺序、验收条件与停止点。
- 建议提交信息：`docs: 编写T17任务指导`

### 2. 补齐完整 Trace 与确定性回放

- 审查一次完整会话能否串联：会话标识、用户输入、模型输出或安全决策摘要、工具动作、策略判定、审批、Observation、Hook、Sensor、Checkpoint、Memory 生命周期、父子 Agent 关系及停止原因。
- 仅补当前实现确实缺失的最小字段或事件，保持旧 Trace 的兼容读取与迁移能力。
- Trace 必须有大小边界并持续脱敏，禁止记录凭据和不必要的敏感正文。
- 为完整链路、脱敏、旧版本兼容和确定性回放补充测试。
- 建议提交信息：`feat: 完善最终Trace与回放链路`

### 3. 建立最终离线验收矩阵

- 以 `docs/assessments/FULL_HARNESS_REASSESSMENT.md` 中的验收场景为基准，补齐可重复的自动化测试。
- 至少覆盖：全新工作区首次启动、会话连续对话、重启后 Memory、Skill、Mock MCP、Hook、子 Agent、Sensor、Checkpoint、Trace 脱敏及失败停止。
- Provider、凭据存储和外部工具全部使用 deterministic mock/stub，不依赖网络或真实 Key。
- 避免重复已有单测，优先增加少量端到端验收用例。
- 建议提交信息：`test: 补齐Harness最终离线验收矩阵`

### 4. 生成并验证 v2.0.0 CLI 分发包

- 将需要公开呈现的版本号统一更新为 `2.0.0`，同步包清单、锁文件与版本文档。
- 确认 tarball 只包含运行 CLI 所需的构建产物、入口、包元数据和必要说明，不包含源码工作区垃圾、凭据、运行记忆或 Trace。
- 使用统一 `pack` 入口构建，在全新临时目录中安装或解包，并完成离线 `smoke` 与启动入口验证。
- 除非仓库既有交付策略明确要求，不提交生成的 `.tgz`；由 CI/Release 产出正式附件。
- 建议提交信息：`build: 准备v2.0.0 CLI分发包`

### 5. 完成助教可独立使用的交付文档

- README 应让首次接触项目的助教仅凭 Release 页面即可完成：环境检查、下载、安装、首次配置、启动、连续对话、退出与再次进入。
- 明确首次配置只要求用户填写服务地址、API Key 和模型名称；工作区默认为启动命令所在目录；API Key 只进入本地安全存储。
- 简明列出会话、Memory、Skill、MCP、Hook、子 Agent、Sensor、Checkpoint、Trace 的可验证方式及限制。
- 明确部署方案为“CLI + 托管平台 Release 链接”，无需公开 WebUI；未知链接使用待填写标记，不伪造 URL。
- 更新 `PLAN.md`、`SPEC.md`、开发记录、反思及提交说明中与当前状态、旧密码流程、旧版本号或过时计划冲突的内容。
- 准备可直接粘贴到公开 Release 的标题、说明、附件名、校验和说明和验收步骤。
- 建议提交信息：`docs: 完成v2.0.0最终交付说明`

### 6. 执行最终离线门禁并收尾

- 依次运行统一入口的 `all`、`pack`、全新目录分发包验证和 `audit`；修复本任务引入的问题。
- 检查 `git diff --check`、工作区状态、包内容和文档链接，不留下生成缓存或敏感文件。
- 在 `PLAN.md` 中记录 T17 离线验收结果，并说明仍待总控完成：真实 Provider 验收、合并 `dev`、标签、推送与公开 Release。
- 最后清空 `guiding.md` 并提交，保持分支干净，等待总控检查。
- 建议提交信息：`chore: 收尾T17最终交付准备`

## 完成标准

- T17 分支提交数不超过 7，工作区干净。
- 完整 Harness 链路可由离线自动化测试稳定复现，Trace 可审计且不泄露凭据。
- `v2.0.0` CLI 包可从全新目录运行，助教无需阅读仓库源码即可按文档测试。
- 全部统一门禁通过，Release 标题、说明、附件与校验材料准备完成。
- `guiding.md` 已清空。
- 分支停在合并前；未使用真实 Key，未合并、未打标签、未发布。
