# T13：完整会话上下文与规则装配

## 目标

让 CLI Agent 在同一终端会话中保留完整对话上下文，并自动装配工作区规则；在上下文达到预算时进行确定性压缩。完成后停在本分支，等待项目负责人手动合并回 `dev`。

## 固定边界

- 分支：`feat/t13-session-context-rules`。
- 以 `docs/assessments/FULL_HARNESS_REASSESSMENT.md` 第 5、6、7 节为准。
- 本任务只处理会话上下文、`/new`、`/model`、规则加载和上下文压缩。
- 不实现长期 Memory、Skill、MCP、Hooks、子 Agent、传感器或 Checkpoint；这些属于 T14–T16。
- 当前运行目录继续作为工作区；普通流程不得要求用户编辑配置或输入本地保护密码。
- 不得读取、记录或提交真实 API Key。
- 本分支总提交数不得超过 7 个；首提交仅写本文件。

## 提交步骤

### 提交 1：任务指导

- 写入本 `guiding.md`。
- 不修改产品代码。

### 提交 2：建立完整会话消息模型

- 让同一交互会话持续保存 user、assistant、action 和 observation。
- 保持 mock/stub LLM 可注入和确定性测试能力。
- Trace 与模型上下文职责分离，不能用 Trace 冒充会话历史。
- 先添加失败测试，再完成最小实现。

### 提交 3：接通 CLI 会话命令

- 后续自然语言输入复用当前会话上下文，不再创建互不相关的独立对话。
- `/new` 清空当前短期会话并开始新对话，但不得误删长期数据。
- `/model` 能查看当前模型，也能直接填写并保存新的模型名称。
- 普通输出继续隐藏 Action JSON、底层工具名和内部错误码。

### 提交 4：自动装配工作区规则

- 启动时自动发现并加载适用范围内的 `AGENTS.md` 和 `CLAUDE.md`。
- 明确规则优先级、作用域和稳定顺序。
- 规则内容进入模型上下文，但不得绕过路径围栏、Policy 或 Approval。
- 对缺失文件、嵌套规则和冲突规则编写离线测试。

### 提交 5：实现确定性上下文压缩

- 达到可配置预算阈值时进行确定性压缩。
- 必须保留系统约束、适用规则、当前目标、摘要和近期消息。
- 不得把 API Key、凭据或未经筛选的大段工具输出写入摘要。
- 测试压缩触发、未触发、重复运行稳定性和压缩后继续调用工具。

### 提交 6：集成验收与收尾

- 验证第二个问题能够引用第一个问题及回答。
- 验证 assistant 文本、Action 和 Observation 都会进入下一轮上下文。
- 验证 `/new`、`/model`、规则加载和压缩的组合行为。
- 使用统一入口运行 `test`、`lint`、`typecheck`、`build` 和 `audit`；若遇到 `spawn EPERM`，获准后原样重跑。
- 更新必要的 README、SPEC、PLAN 和 AGENT_LOG 状态。
- 清空 `guiding.md` 后提交，确保本分支总提交数不超过 7 个。

## 完成标准

1. 同一 CLI 会话的后续输入可以引用此前对话。
2. 完整消息序列包含 user、assistant、action 和 observation。
3. `/new` 与 `/model` 按用户流程工作。
4. 工作区规则自动、稳定且受作用域约束地注入。
5. 上下文超限时确定性压缩，并保留目标、规则、摘要与近期消息。
6. 新机制有 mock/stub 离线测试，既有安全边界不退化。
7. 统一门禁通过，工作树干净，分支停在等待负责人合并的状态。

## 停止条件

- 不合并到 `dev`，不创建 GitLab MR，不发布版本。
- 如果必须扩大到 T14–T16 范围，停止并向项目负责人说明。
- 如果预计超过 7 个提交，先停止并重新拆分，不得强行压缩历史。
