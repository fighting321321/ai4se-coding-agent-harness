# 项目反思

## 范围取舍、工程实践与 AI 协作

这次暑期课程项目让我体会最深的，不只是如何实现一个 Coding Agent Harness，更是如何管理 AI 参与的软件工程过程。项目最后完成了从 Action、LLM 抽象、受限工具、治理策略、Memory、Trace，到 Agent Loop、真实 Provider、CLI、WebUI 和 npm 分发的最小闭环。不过，与这些功能相比，我认为更值得总结的是项目范围如何调整、人与 AI 如何分工，以及一套严谨流程应该怎样根据实际项目进行取舍。

项目开始时，我选择的规划过于复杂。最初的方案把项目拆成了 T01–T20，并考虑了数据库、多用户、Docker、线上后端和更复杂的审批流程。这些设计看起来比较完整，但对于一个时间和资源都有限的暑期课程作业来说并不合适。过多的任务、文档、分支和检查步骤拉长了整体开发周期，也消耗了大量上下文和 token。后来我要求重新对照原始 guide，把目标调整为满足硬性要求的最简单结果，将实现阶段压缩为 T05–T12，明确不实现数据库、多用户、SSE、Docker和线上后端。经过这次调整，我认识到，项目规划并不是越详细、功能越多越好。好的规划首先要符合项目规模，在完成必要要求的基础上保持清楚的停止线。

在 AI 协作方面，我没有把智能体输出的计划直接当成最终决定，而是多次进行纠正和规范。例如，我要求不同的 Txx 必须使用独立分支，不能共用分支；要求每个后续分支不超过七个提交；要求 `guiding.md` 中的每一步与一个提交对应；当过程越来越冗长时，又明确要求转入简易模式。这些调整说明，现在的智能体已经能够承担大量编码、测试、文档整理和问题定位工作，但仍需要人持续提供方向。AI 容易追求形式上的完整，或者在没有明确停止条件时继续扩大范围，因此项目负责人必须判断哪些建议应该采纳，哪些应该推翻。人不能只做需求的提出者，还要负责控制范围、安排顺序并验收最终结果。

对我帮助最大的流程是实现后的自我审查。刚写完代码时进行普通检查，往往只能发现比较明显的问题；当功能形成完整上下文后，再从安全边界、异常输入和跨平台行为等角度集中审查，效率更高，也确实发现了很多真实问题。项目中先后检查出了 Shell 与危险 Git 命令绕过、路径和工作区边界、Memory 与 Trace 的异常结构、HTTP 重定向、凭据脱敏、本地端口处理以及 Windows CRLF 换行等问题。这让我认识到，“代码能够运行”并不等于“实现已经完成”，很多缺陷只有把多个模块连起来，或者主动站在错误输入和攻击者的角度检查时才会出现。

TDD 在本项目中既增加了一些前期步骤，也放大了这种审查能力。先观察 RED，再做最小 GREEN，可以证明测试确实覆盖了缺失行为，而不是实现完成后补一个必然通过的断言。对于课程小项目，如果每个细节都机械地重复完整流程，会让过程变慢；但在命令执行、文件写入、凭据、网络请求和分发这些高风险边界上，TDD 很有价值。因此，我最后的看法不是放弃严谨流程，而是根据风险使用它：核心机制和安全边界保留真实 RED/GREEN，普通文档修改则不必制造没有意义的形式。

分支和任务颗粒度也影响了 AI 能自主工作多久。任务过大时，智能体容易偏离主题；任务过细时，又会产生大量交接和重复检查。本项目后期采用“一个 Txx 一个独立分支、一个 `guiding.md`、最多七个提交”的方式，任务目标和提交边界更清楚。主控对话负责检查整体状态，专门的 Txx 对话负责当前实现，这种上下文隔离减少了不同阶段互相干扰。不过，它并没有让智能体完全脱离人的监督。每次开始新任务前仍要核对前一分支是否真正完成，遇到规划过度或实现偏离时也需要我重新下达约束。

SPEC 和 PLAN 的质量会直接影响实现质量。最明显的例子就是初始计划把课程作业设计成接近完整平台，导致后续任务数量和实现范围不断扩大。将 SPEC 改为“满足原始 guide 的最小 Coding Agent Harness”后，后面的 Action、工具、反馈循环、凭据、WebUI 和分发都有了明确边界。对我来说，最有效的 prompt 和上下文策略不是一次给 AI 大量模糊要求，而是明确当前分支、允许修改的范围、提交数量、完成条件和禁止扩展的功能。约束越具体，AI 的执行越稳定。

凭据安全是另一个重要收获。真实密码和 API Key 千万不能发送到 Git，否则可能造成严重后果，包括配额被盗用、账号或数据泄漏，而且即使删除当前文件，敏感信息仍可能保留在 Git 历史中。因此，项目使用隐藏输入、加密凭据文件、日志和 Trace 脱敏、测试假 Key、重定向限制以及当前文件与历史扫描。真实 API 测试也只通过受控的本地入口完成。这个过程让我意识到，安全不能等功能完成后再补，而要从输入、内存、日志、文件、网络和版本历史多个环节同时考虑。

分发要求则让我认识到，开发目录中能够运行还不算真正交付。项目最终需要生成 npm tarball，并在全新临时目录中完成安装、ESM 导入和离线命令 smoke。它迫使我检查构建产物、入口、类型声明和包内容是否完整。相比只提交源码，这种验证更接近用户实际拿到项目后的使用方式。

最终部署也让我看到“流水线成功”和“用户真正可访问”并不是一回事。GitLab 中静态页面构建和 Pages 作业连续通过，但学校实例没有生成公开地址。继续修改代码只会重复消耗时间，因此我根据助教允许 CLI 项目使用托管平台 Release 的补充说明，决定停止 Pages 尝试，先用 GitLab Release 验证流程，随后因学校网络不稳定将最终公开入口迁移到 GitHub Release。这个过程提醒我：智能体容易把绿色作业当成部署完成，而负责人必须检查最终用户入口，并在外部平台能力不足时及时调整交付方案。

如果重新开始，我会更早从原始 guide 的硬性要求出发，先确定最小闭环，再决定是否增加功能，避免一开始建立超出课程规模的复杂方案。同时，我仍会继续使用现在这套经过调整的严谨流程：独立分支、明确 guiding、有限提交、TDD、自我审查和最终验证。它的价值不在于步骤越多越好，而在于让每个关键结论都有证据。Superpowers 方法论假设项目拥有足够的时间、上下文和审查资源，这在小型课程作业中并不完全成立；但只要由人控制颗粒度、删掉形式大于实质的部分，它仍然能够显著提高实现的可靠性。

总的来说，这次项目让我认识到，AI 最适合成为执行能力很强的协作者，而不是不受约束的项目负责人。有效使用 AI 的关键不是给它最大自由，而是由人明确目标、及时纠偏、控制范围，并用测试和审查验证结果。我认为自己现在采用的 AI 协作方式是有效的，以后仍会继续使用这套严谨流程，同时根据项目规模主动简化它。

---

本文内容、经历与观点由项目负责人本人提供，AI 仅辅助结构整理、语言润色和格式检查，未新增个人经历或结论。

> 交付状态附注（2026-08-04，由 AI 根据客观构建记录整理，不属于负责人反思正文）：最终包为 `ai4se-harness-2.1.1.tgz`，已通过全新目录离线安装、smoke 与真实 Provider 只读验收，并已随 SHA-256 校验文件发布至 GitHub Release。
