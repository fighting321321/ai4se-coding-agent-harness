{
  "name": "ai4se",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.14.0",
  "engines": {
    "node": ">=24.0.0 <25.0.0",
    "pnpm": "11.14.0"
  },
  "scripts": {
    "test": "vitest run --config vitest.workspace.ts",
    "demo": "vitest run tests/integration/demos/mechanisms.test.ts",
    "lint": "eslint .",
    "typecheck": "pnpm --filter @ai4se/api run typecheck && pnpm --filter @ai4se/web run typecheck && pnpm --filter @ai4se/harness run typecheck && tsc --noEmit -p tests/tsconfig.json",
    "build": "pnpm --filter @ai4se/harness run build && pnpm --filter @ai4se/api run build && pnpm --filter @ai4se/web run build",
    "agent": "pnpm --filter @ai4se/harness run build && pnpm --filter @ai4se/api run build && node apps/api/dist/cli-entry.js"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "24.13.3",
    "eslint": "10.7.0",
    "typescript": "6.0.3",
    "typescript-eslint": "8.64.0",
    "vitest": "4.1.10"
  }
}
